# Auto-generated by cmd/gen-features — do NOT edit by hand.
# Regenerate with: make features-gen
reconcilers:
    - name: IAMConfig
      package: iamconfig
      description: Reconciles IAMConfig CRs and propagates effective config.
      kinds:
        - IAMConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: S3Config
      package: s3config
      description: Reconciles S3Config CRs and propagates effective config.
      kinds:
        - S3Config
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: KMSConfig
      package: kmsconfig
      description: Reconciles KMSConfig CRs and propagates effective config.
      kinds:
        - KMSConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: SQSConfig
      package: sqsconfig
      description: Reconciles SQSConfig CRs and propagates effective config.
      kinds:
        - SQSConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: SecretsManagerConfig
      package: secretsmanagerconfig
      description: Reconciles SecretsManagerConfig CRs and propagates effective config.
      kinds:
        - SecretsManagerConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: PolicyDocument
      package: policydocument
      description: Reconciles PolicyDocument CRs and resolves IAM policy references.
      kinds:
        - PolicyDocument
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: LabelOperator
      package: labeloperator
      description: Applies provider resource-name labels to all CRs across all provider API groups.
      sinceVersion: v0.0.1
      stability: stable
    - name: SNSConfig
      package: snsconfig
      description: Reconciles SNSConfig CRs and propagates effective config.
      kinds:
        - SNSConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: DynamoDBConfig
      package: dynamodbconfig
      description: Reconciles DynamoDBConfig CRs and propagates effective config.
      kinds:
        - DynamoDBConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: EventBridgeConfig
      package: eventbridgeconfig
      description: Reconciles EventBridgeConfig CRs and propagates effective config.
      kinds:
        - EventBridgeConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: CloudWatchLogsConfig
      package: cloudwatchlogsconfig
      description: Reconciles CloudWatchLogsConfig CRs and propagates effective config.
      kinds:
        - CloudWatchLogsConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: CloudWatchConfig
      package: cloudwatchconfig
      description: Reconciles CloudWatchConfig CRs and propagates effective config.
      kinds:
        - CloudWatchConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: ELBConfig
      package: elbconfig
      description: Reconciles ELBConfig CRs and propagates effective config.
      kinds:
        - ELBConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: RDSConfig
      package: rdsconfig
      description: Reconciles RDSConfig CRs and propagates effective config.
      kinds:
        - RDSConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: AutoScalingConfig
      package: autoscalingconfig
      description: Reconciles AutoScalingConfig CRs and propagates effective config.
      kinds:
        - AutoScalingConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: ECSConfig
      package: ecsconfig
      description: Reconciles ECSConfig CRs and propagates effective config.
      kinds:
        - ECSConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: EKSConfig
      package: eksconfig
      description: Reconciles EKSConfig CRs and propagates effective config.
      kinds:
        - EKSConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: EC2Config
      package: ec2config
      description: Reconciles EC2Config CRs and propagates effective config.
      kinds:
        - EC2Config
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: ApiGatewayV2Config
      package: apigatewayv2config
      description: Reconciles ApiGatewayV2Config CRs and propagates effective config.
      kinds:
        - ApiGatewayV2Config
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: APIGatewayConfig
      package: apigatewayconfig
      description: Reconciles APIGatewayConfig CRs and propagates effective config.
      kinds:
        - APIGatewayConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: EFSConfig
      package: efsconfig
      description: Reconciles EFSConfig CRs and propagates effective config.
      kinds:
        - EFSConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: ElastiCacheConfig
      package: elasticacheconfig
      description: Reconciles ElastiCacheConfig CRs and propagates effective config.
      kinds:
        - ElastiCacheConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: ECRConfig
      package: ecrconfig
      description: Reconciles ECRConfig CRs and propagates effective config.
      kinds:
        - ECRConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: StepFunctionsConfig
      package: stepfunctionsconfig
      description: Reconciles StepFunctionsConfig CRs and propagates effective config.
      kinds:
        - StepFunctionsConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: MSKConfig
      package: mskconfig
      description: Reconciles MSKConfig CRs and propagates effective config.
      kinds:
        - MSKConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: MemoryDBConfig
      package: memorydbconfig
      description: Reconciles MemoryDBConfig CRs and propagates effective config.
      kinds:
        - MemoryDBConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: ACMConfig
      package: acmconfig
      description: Reconciles ACMConfig CRs and propagates effective config.
      kinds:
        - ACMConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: EMRConfig
      package: emrconfig
      description: Reconciles EMRConfig CRs and propagates effective config.
      kinds:
        - EMRConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: DocumentDBConfig
      package: documentdbconfig
      description: Reconciles DocumentDBConfig CRs and propagates effective config.
      kinds:
        - DocumentDBConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: GlueConfig
      package: glueconfig
      description: Reconciles GlueConfig CRs and propagates effective config.
      kinds:
        - GlueConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: AthenaConfig
      package: athenaconfig
      description: Reconciles AthenaConfig CRs and propagates effective config.
      kinds:
        - AthenaConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: DSQLConfig
      package: dsqlconfig
      description: Reconciles DSQLConfig CRs and propagates effective config.
      kinds:
        - DSQLConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: Route53Config
      package: route53config
      description: Reconciles Route53Config CRs and propagates effective config.
      kinds:
        - Route53Config
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: SSMConfig
      package: ssmconfig
      description: Reconciles SSMConfig CRs and propagates effective config.
      kinds:
        - SSMConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: CognitoConfig
      package: cognitoconfig
      description: Reconciles CognitoConfig CRs and propagates effective config.
      kinds:
        - CognitoConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: KinesisConfig
      package: kinesisconfig
      description: Reconciles KinesisConfig CRs and propagates effective config.
      kinds:
        - KinesisConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
    - name: CloudTrailConfig
      package: cloudtrailconfig
      description: Reconciles CloudTrailConfig CRs and propagates effective config.
      kinds:
        - CloudTrailConfig
        - KropathConfig
      sinceVersion: v0.0.1
      stability: stable
