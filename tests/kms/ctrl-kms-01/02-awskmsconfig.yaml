# Shared empty local AWSKMSConfig — all cascade sources come from global configs.
apiVersion: aws.kropath.run/v1alpha1
kind: AWSKMSConfig
metadata:
  name: general-policy
  namespace: payments-prod
spec:
  mandatory: {}
  defaults: {}
