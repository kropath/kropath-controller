# Assert AC-15: effCfg.mandatory.tags is the union of KropathConfig.mandatory.tags
# and KMSConfig.mandatory.tags.
apiVersion: aws.kropath.run/v1alpha1
kind: KMSConfig
metadata:
  name: ac15-general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    mandatory:
      tags:
        cost-centre: security
        key-type: encryption
