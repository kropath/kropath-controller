# Assert AC-15: effCfg.mandatory.tags is the union of KropathConfig.mandatory.tags
# and AWSKMSConfig.mandatory.tags.
apiVersion: kropath.run/v1alpha1
kind: AWSKMSConfig
metadata:
  name: general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    mandatory:
      tags:
        cost-centre: security
        key-type: encryption
