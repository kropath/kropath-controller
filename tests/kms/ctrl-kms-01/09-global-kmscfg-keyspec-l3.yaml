# AC-4: globalAWSKMSConfig mandatory.keySpec="SYMMETRIC_DEFAULT" (level 3).
apiVersion: aws.kropath.run/v1alpha1
kind: AWSKMSConfig
metadata:
  name: general-policy
  namespace: kro-system
spec:
  mandatory:
    keySpec: "SYMMETRIC_DEFAULT"
  defaults: {}
