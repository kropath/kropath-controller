# AC-4: globalKMSConfig mandatory.keySpec="SYMMETRIC_DEFAULT" (level 3).
apiVersion: aws.kropath.run/v1alpha1
kind: KMSConfig
metadata:
  name: ac4-general-policy
  namespace: kro-system
spec:
  mandatory:
    keySpec: "SYMMETRIC_DEFAULT"
  defaults: {}
---
# AC-4: local KMSConfig mandatory.keySpec="RSA_4096" (level 4 — should lose to level 3).
apiVersion: aws.kropath.run/v1alpha1
kind: KMSConfig
metadata:
  name: ac4-general-policy
  namespace: payments-prod
spec:
  mandatory:
    keySpec: "RSA_4096"
  defaults: {}
