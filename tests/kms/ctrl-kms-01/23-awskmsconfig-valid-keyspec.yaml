# AC-10: AWSKMSConfig with mandatory.keySpec IN mandatory.allowedKeySpecs.
# Controller must set Valid=True and write effectiveConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: AWSKMSConfig
metadata:
  name: general-policy
  namespace: payments-prod
spec:
  mandatory:
    keySpec: "SYMMETRIC_DEFAULT"
    allowedKeySpecs:
      - "SYMMETRIC_DEFAULT"
      - "RSA_4096"
  defaults: {}
