# AC-6: globalKMSConfig mandatory.keyUsage="ENCRYPT_DECRYPT" (level 3).
apiVersion: aws.kropath.run/v1alpha1
kind: KMSConfig
metadata:
  name: general-policy
  namespace: kro-system
spec:
  mandatory:
    keyUsage: "ENCRYPT_DECRYPT"
  defaults: {}
