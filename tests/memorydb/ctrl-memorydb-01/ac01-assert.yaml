# Assert AC-1: mandatory.tlsEnabled=true from level-1 globalKropathConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: MemoryDBConfig
metadata:
  name: ac1-mdb-policy
  namespace: payments-prod
status:
  effectiveConfig:
    mandatory:
      tlsEnabled: true
