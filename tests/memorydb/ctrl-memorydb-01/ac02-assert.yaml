# Assert AC-2: mandatory.tlsEnabled=true from level-4 localMemoryDBConfig.mandatory.
apiVersion: aws.kropath.run/v1alpha1
kind: MemoryDBConfig
metadata:
  name: ac2-mdb-policy
  namespace: payments-prod
status:
  effectiveConfig:
    mandatory:
      tlsEnabled: true
