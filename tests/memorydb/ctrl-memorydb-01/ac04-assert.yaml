# Assert AC-4: mandatory.allowedNodeTypes from level-1 globalKropathConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: MemoryDBConfig
metadata:
  name: ac4-mdb-policy
  namespace: payments-prod
status:
  effectiveConfig:
    mandatory:
      allowedNodeTypes:
        - "db.r7g.large"
        - "db.r7g.xlarge"
