# Assert CC-8: effCfg.defaults.namingTemplate={namespace}-{name} from globalMQConfig (level 7).
apiVersion: aws.kropath.run/v1alpha1
kind: MQConfig
metadata:
  name: ac8-mq-policy
  namespace: ac08-mq-app-ns
status:
  effectiveConfig:
    defaults:
      namingTemplate: "{namespace}-{name}"
