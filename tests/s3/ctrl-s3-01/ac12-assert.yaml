apiVersion: aws.kropath.run/v1alpha1
kind: S3Config
metadata:
  name: ac12-ac12-general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    mandatory:
      namingTemplate: "{account_id}-{namespace}-{name}"
