# Global StepFunctionsConfig (kro-system) — sets mandatory.syncedLabels (level 3).
apiVersion: aws.kropath.run/v1alpha1
kind: StepFunctionsConfig
metadata:
  name: general-sfn
  namespace: sfn-global-ns
spec:
  mandatory:
    syncedLabels:
      team: platform
