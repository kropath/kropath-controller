# Global StepFunctionsConfig (kro-system) — sets mandatory.tags (level 3).
apiVersion: aws.kropath.run/v1alpha1
kind: StepFunctionsConfig
metadata:
  name: general-sfn
  namespace: sfn-global-ns
spec:
  mandatory:
    tags:
      cost-centre: platform
