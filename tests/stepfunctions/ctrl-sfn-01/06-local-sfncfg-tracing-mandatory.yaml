# Local StepFunctionsConfig (stepfunctions) — sets mandatory.tracingEnabled=true (level 4).
apiVersion: aws.kropath.run/v1alpha1
kind: StepFunctionsConfig
metadata:
  name: general-sfn
  namespace: sfn-app-ns
spec:
  mandatory:
    tracingEnabled: true
