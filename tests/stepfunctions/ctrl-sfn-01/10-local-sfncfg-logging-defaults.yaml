# Local StepFunctionsConfig (stepfunctions) — sets defaults.loggingLevel="FATAL" (level 6).
apiVersion: aws.kropath.run/v1alpha1
kind: StepFunctionsConfig
metadata:
  name: general-sfn
  namespace: sfn-app-ns
spec:
  defaults:
    loggingLevel: "FATAL"
