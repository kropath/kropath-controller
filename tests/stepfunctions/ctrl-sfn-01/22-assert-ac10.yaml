# AC-10: effectiveConfig.mandatory.namingTemplate from globalSFNConfig.mandatory (L3).
apiVersion: aws.kropath.run/v1alpha1
kind: StepFunctionsConfig
metadata:
  name: general-sfn
  namespace: stepfunctions
status:
  effectiveConfig:
    mandatory:
      namingTemplate: "{namespace}-{name}"
