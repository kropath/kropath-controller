# Global StepFunctionsConfig (kro-system) — sets mandatory.namingTemplate (level 3).
apiVersion: aws.kropath.run/v1alpha1
kind: StepFunctionsConfig
metadata:
  name: general-sfn
  namespace: kro-system
spec:
  mandatory:
    namingTemplate: "{namespace}-{name}"
