# AC-6: effectiveConfig.defaults.tracingEnabled=false when
# globalSFNConfig.defaults.tracingEnabled=false (L7, explicit false).
apiVersion: aws.kropath.run/v1alpha1
kind: StepFunctionsConfig
metadata:
  name: general-sfn
  namespace: stepfunctions
status:
  effectiveConfig:
    defaults:
      tracingEnabled: false
