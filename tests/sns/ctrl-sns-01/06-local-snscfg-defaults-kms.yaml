# AC-3: localSNSConfig with defaults.kmsMasterKeyId="alias/aws/sns" (level 6).
apiVersion: aws.kropath.run/v1alpha1
kind: SNSConfig
metadata:
  name: general-policy
  namespace: events-prod
spec:
  mandatory: {}
  defaults:
    kmsMasterKeyId: "alias/aws/sns"
