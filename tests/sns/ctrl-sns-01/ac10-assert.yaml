# Assert AC-10: defaults.namingTemplate="{namespace}-{name}" from level-7 globalSNSConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: SNSConfig
metadata:
  name: ac10-general-policy
  namespace: events-prod
status:
  effectiveConfig:
    defaults:
      namingTemplate: "{namespace}-{name}"
