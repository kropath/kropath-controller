# Assert AC-12: mandatory.syncedLabels from level-3 globalSNSConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: SNSConfig
metadata:
  name: ac12-general-policy
  namespace: events-prod
status:
  effectiveConfig:
    mandatory:
      syncedLabels:
        data-class: internal
