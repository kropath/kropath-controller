# Assert AC-5: defaults.signatureVersion="2" (level 7 beats level 9).
apiVersion: aws.kropath.run/v1alpha1
kind: SNSConfig
metadata:
  name: general-policy
  namespace: events-prod
status:
  effectiveConfig:
    defaults:
      signatureVersion: "2"
