# Copyright 2026 kropath Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kropathconfigs.aws.kropath.run
spec:
  group: aws.kropath.run
  names:
    kind: KropathConfig
    listKind: KropathConfigList
    plural: kropathconfigs
    singular: kropathconfig
    shortNames:
      - kpc
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Age
          type: date
          jsonPath: ".metadata.creationTimestamp"
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-validations:
            # S3 field mutual-exclusivity: a field must not appear in both
            # mandatory.s3 and defaults.s3 simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.s3) &&
                  has(self.spec.mandatory.s3.encryptionAlgorithm) &&
                  self.spec.mandatory.s3.encryptionAlgorithm != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.s3) &&
                  has(self.spec.defaults.s3.encryptionAlgorithm) &&
                  self.spec.defaults.s3.encryptionAlgorithm != "")
              message: >-
                spec.mandatory.s3.encryptionAlgorithm and
                spec.defaults.s3.encryptionAlgorithm cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.s3) &&
                  has(self.spec.mandatory.s3.kmsKeyArn) &&
                  self.spec.mandatory.s3.kmsKeyArn != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.s3) &&
                  has(self.spec.defaults.s3.kmsKeyArn) &&
                  self.spec.defaults.s3.kmsKeyArn != "")
              message: >-
                spec.mandatory.s3.kmsKeyArn and
                spec.defaults.s3.kmsKeyArn cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.s3) &&
                  has(self.spec.mandatory.s3.blockPublicAccess) &&
                  self.spec.mandatory.s3.blockPublicAccess == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.s3) &&
                  has(self.spec.defaults.s3.blockPublicAccess) &&
                  self.spec.defaults.s3.blockPublicAccess == true)
              message: >-
                spec.mandatory.s3.blockPublicAccess and
                spec.defaults.s3.blockPublicAccess cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.s3) &&
                  has(self.spec.mandatory.s3.versioning) &&
                  self.spec.mandatory.s3.versioning != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.s3) &&
                  has(self.spec.defaults.s3.versioning) &&
                  self.spec.defaults.s3.versioning != "")
              message: >-
                spec.mandatory.s3.versioning and
                spec.defaults.s3.versioning cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.s3) &&
                  has(self.spec.mandatory.s3.loggingEnabled) &&
                  self.spec.mandatory.s3.loggingEnabled == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.s3) &&
                  has(self.spec.defaults.s3.loggingEnabled) &&
                  self.spec.defaults.s3.loggingEnabled == true)
              message: >-
                spec.mandatory.s3.loggingEnabled and
                spec.defaults.s3.loggingEnabled cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.s3) &&
                  has(self.spec.mandatory.s3.logDeliveryBucket) &&
                  self.spec.mandatory.s3.logDeliveryBucket != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.s3) &&
                  has(self.spec.defaults.s3.logDeliveryBucket) &&
                  self.spec.defaults.s3.logDeliveryBucket != "")
              message: >-
                spec.mandatory.s3.logDeliveryBucket and
                spec.defaults.s3.logDeliveryBucket cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.s3) &&
                  has(self.spec.mandatory.s3.enforceHttpsOnly) &&
                  self.spec.mandatory.s3.enforceHttpsOnly == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.s3) &&
                  has(self.spec.defaults.s3.enforceHttpsOnly) &&
                  self.spec.defaults.s3.enforceHttpsOnly == true)
              message: >-
                spec.mandatory.s3.enforceHttpsOnly and
                spec.defaults.s3.enforceHttpsOnly cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.s3) &&
                  has(self.spec.mandatory.s3.objectLockMode) &&
                  self.spec.mandatory.s3.objectLockMode != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.s3) &&
                  has(self.spec.defaults.s3.objectLockMode) &&
                  self.spec.defaults.s3.objectLockMode != "")
              message: >-
                spec.mandatory.s3.objectLockMode and
                spec.defaults.s3.objectLockMode cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.s3) &&
                  has(self.spec.mandatory.s3.objectLockRetentionDays) &&
                  self.spec.mandatory.s3.objectLockRetentionDays != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.s3) &&
                  has(self.spec.defaults.s3.objectLockRetentionDays) &&
                  self.spec.defaults.s3.objectLockRetentionDays != 0)
              message: >-
                spec.mandatory.s3.objectLockRetentionDays and
                spec.defaults.s3.objectLockRetentionDays cannot both be set.
                Set the field in exactly one tier.
            # KMS field mutual-exclusivity: a field must not appear in both
            # mandatory.kms and defaults.kms simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.kms) &&
                  has(self.spec.mandatory.kms.enableKeyRotation) &&
                  self.spec.mandatory.kms.enableKeyRotation == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.kms) &&
                  has(self.spec.defaults.kms.enableKeyRotation) &&
                  self.spec.defaults.kms.enableKeyRotation == true)
              message: >-
                spec.mandatory.kms.enableKeyRotation and
                spec.defaults.kms.enableKeyRotation cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.kms) &&
                  has(self.spec.mandatory.kms.allowedKeySpecs) &&
                  size(self.spec.mandatory.kms.allowedKeySpecs) > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.kms) &&
                  has(self.spec.defaults.kms.allowedKeySpecs) &&
                  size(self.spec.defaults.kms.allowedKeySpecs) > 0)
              message: >-
                spec.mandatory.kms.allowedKeySpecs and
                spec.defaults.kms.allowedKeySpecs cannot both be set.
                Set the field in exactly one tier.
            # IAM field mutual-exclusivity: a field must not appear in both
            # mandatory.iam and defaults.iam simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.iam) &&
                  has(self.spec.mandatory.iam.permissionsBoundaryArn) &&
                  self.spec.mandatory.iam.permissionsBoundaryArn != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.iam) &&
                  has(self.spec.defaults.iam.permissionsBoundaryArn) &&
                  self.spec.defaults.iam.permissionsBoundaryArn != "")
              message: >-
                spec.mandatory.iam.permissionsBoundaryArn and
                spec.defaults.iam.permissionsBoundaryArn cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.iam) &&
                  has(self.spec.mandatory.iam.blockIamUserAccessKeys) &&
                  self.spec.mandatory.iam.blockIamUserAccessKeys == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.iam) &&
                  has(self.spec.defaults.iam.blockIamUserAccessKeys) &&
                  self.spec.defaults.iam.blockIamUserAccessKeys == true)
              message: >-
                spec.mandatory.iam.blockIamUserAccessKeys and
                spec.defaults.iam.blockIamUserAccessKeys cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.iam) &&
                  has(self.spec.mandatory.iam.maxSessionDurationSeconds) &&
                  self.spec.mandatory.iam.maxSessionDurationSeconds != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.iam) &&
                  has(self.spec.defaults.iam.maxSessionDurationSeconds) &&
                  self.spec.defaults.iam.maxSessionDurationSeconds != 0)
              message: >-
                spec.mandatory.iam.maxSessionDurationSeconds and
                spec.defaults.iam.maxSessionDurationSeconds cannot both be non-zero.
                Set the field in exactly one tier.
            # SQS field mutual-exclusivity: a field must not appear in both
            # mandatory.sqs and defaults.sqs simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sqs) &&
                  has(self.spec.mandatory.sqs.encryptionType) &&
                  self.spec.mandatory.sqs.encryptionType != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.sqs) &&
                  has(self.spec.defaults.sqs.encryptionType) &&
                  self.spec.defaults.sqs.encryptionType != "")
              message: >-
                spec.mandatory.sqs.encryptionType and
                spec.defaults.sqs.encryptionType cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sqs) &&
                  has(self.spec.mandatory.sqs.kmsMasterKeyId) &&
                  self.spec.mandatory.sqs.kmsMasterKeyId != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.sqs) &&
                  has(self.spec.defaults.sqs.kmsMasterKeyId) &&
                  self.spec.defaults.sqs.kmsMasterKeyId != "")
              message: >-
                spec.mandatory.sqs.kmsMasterKeyId and
                spec.defaults.sqs.kmsMasterKeyId cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sqs) &&
                  has(self.spec.mandatory.sqs.visibilityTimeout) &&
                  self.spec.mandatory.sqs.visibilityTimeout != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.sqs) &&
                  has(self.spec.defaults.sqs.visibilityTimeout) &&
                  self.spec.defaults.sqs.visibilityTimeout != 0)
              message: >-
                spec.mandatory.sqs.visibilityTimeout and
                spec.defaults.sqs.visibilityTimeout cannot both be non-zero.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sqs) &&
                  has(self.spec.mandatory.sqs.messageRetentionPeriod) &&
                  self.spec.mandatory.sqs.messageRetentionPeriod != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.sqs) &&
                  has(self.spec.defaults.sqs.messageRetentionPeriod) &&
                  self.spec.defaults.sqs.messageRetentionPeriod != 0)
              message: >-
                spec.mandatory.sqs.messageRetentionPeriod and
                spec.defaults.sqs.messageRetentionPeriod cannot both be non-zero.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sqs) &&
                  has(self.spec.mandatory.sqs.delaySeconds) &&
                  self.spec.mandatory.sqs.delaySeconds != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.sqs) &&
                  has(self.spec.defaults.sqs.delaySeconds) &&
                  self.spec.defaults.sqs.delaySeconds != 0)
              message: >-
                spec.mandatory.sqs.delaySeconds and
                spec.defaults.sqs.delaySeconds cannot both be non-zero.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sqs) &&
                  has(self.spec.mandatory.sqs.maximumMessageSize) &&
                  self.spec.mandatory.sqs.maximumMessageSize != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.sqs) &&
                  has(self.spec.defaults.sqs.maximumMessageSize) &&
                  self.spec.defaults.sqs.maximumMessageSize != 0)
              message: >-
                spec.mandatory.sqs.maximumMessageSize and
                spec.defaults.sqs.maximumMessageSize cannot both be non-zero.
                Set the field in exactly one tier.
            # SecretsManager field mutual-exclusivity: kmsKeyID must not appear in both
            # mandatory.secretsManager and defaults.secretsManager simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.secretsManager) &&
                  has(self.spec.mandatory.secretsManager.kmsKeyID) &&
                  self.spec.mandatory.secretsManager.kmsKeyID != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.secretsManager) &&
                  has(self.spec.defaults.secretsManager.kmsKeyID) &&
                  self.spec.defaults.secretsManager.kmsKeyID != "")
              message: >-
                spec.mandatory.secretsManager.kmsKeyID and
                spec.defaults.secretsManager.kmsKeyID cannot both be set.
                Set the field in exactly one tier.
            # SSM field mutual-exclusivity: parameterType, parameterTier, keyID must not appear in both
            # mandatory.ssm and defaults.ssm simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ssm) &&
                  has(self.spec.mandatory.ssm.parameterType) &&
                  self.spec.mandatory.ssm.parameterType != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.ssm) &&
                  has(self.spec.defaults.ssm.parameterType) &&
                  self.spec.defaults.ssm.parameterType != "")
              message: >-
                spec.mandatory.ssm.parameterType and
                spec.defaults.ssm.parameterType cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ssm) &&
                  has(self.spec.mandatory.ssm.parameterTier) &&
                  self.spec.mandatory.ssm.parameterTier != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.ssm) &&
                  has(self.spec.defaults.ssm.parameterTier) &&
                  self.spec.defaults.ssm.parameterTier != "")
              message: >-
                spec.mandatory.ssm.parameterTier and
                spec.defaults.ssm.parameterTier cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ssm) &&
                  has(self.spec.mandatory.ssm.keyID) &&
                  self.spec.mandatory.ssm.keyID != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.ssm) &&
                  has(self.spec.defaults.ssm.keyID) &&
                  self.spec.defaults.ssm.keyID != "")
              message: >-
                spec.mandatory.ssm.keyID and
                spec.defaults.ssm.keyID cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ssm) &&
                  has(self.spec.mandatory.ssm.allowedDocumentTypes) &&
                  size(self.spec.mandatory.ssm.allowedDocumentTypes) > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.ssm) &&
                  has(self.spec.defaults.ssm.allowedDocumentTypes) &&
                  size(self.spec.defaults.ssm.allowedDocumentTypes) > 0)
              message: >-
                spec.mandatory.ssm.allowedDocumentTypes and
                spec.defaults.ssm.allowedDocumentTypes cannot both be set.
                Set the field in exactly one tier.
            # Prevent setting sns fields in both mandatory and defaults simultaneously.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sns) &&
                  has(self.spec.mandatory.sns.kmsMasterKeyId) &&
                  self.spec.mandatory.sns.kmsMasterKeyId != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.sns) &&
                  has(self.spec.defaults.sns.kmsMasterKeyId) &&
                  self.spec.defaults.sns.kmsMasterKeyId != "")
              message: >-
                spec.mandatory.sns.kmsMasterKeyId and
                spec.defaults.sns.kmsMasterKeyId cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sns) &&
                  has(self.spec.mandatory.sns.signatureVersion) &&
                  self.spec.mandatory.sns.signatureVersion != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.sns) &&
                  has(self.spec.defaults.sns.signatureVersion) &&
                  self.spec.defaults.sns.signatureVersion != "")
              message: >-
                spec.mandatory.sns.signatureVersion and
                spec.defaults.sns.signatureVersion cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sns) &&
                  has(self.spec.mandatory.sns.tracingConfig) &&
                  self.spec.mandatory.sns.tracingConfig != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.sns) &&
                  has(self.spec.defaults.sns.tracingConfig) &&
                  self.spec.defaults.sns.tracingConfig != "")
              message: >-
                spec.mandatory.sns.tracingConfig and
                spec.defaults.sns.tracingConfig cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.dynamodb) &&
                  has(self.spec.mandatory.dynamodb.encryptionEnabled) &&
                  has(self.spec.defaults) && has(self.spec.defaults.dynamodb) &&
                  has(self.spec.defaults.dynamodb.encryptionEnabled))
              message: >-
                spec.mandatory.dynamodb.encryptionEnabled and
                spec.defaults.dynamodb.encryptionEnabled cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.dynamodb) &&
                  has(self.spec.mandatory.dynamodb.deletionProtectionEnabled) &&
                  has(self.spec.defaults) && has(self.spec.defaults.dynamodb) &&
                  has(self.spec.defaults.dynamodb.deletionProtectionEnabled))
              message: >-
                spec.mandatory.dynamodb.deletionProtectionEnabled and
                spec.defaults.dynamodb.deletionProtectionEnabled cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.dynamodb) &&
                  has(self.spec.mandatory.dynamodb.pointInTimeRecoveryEnabled) &&
                  has(self.spec.defaults) && has(self.spec.defaults.dynamodb) &&
                  has(self.spec.defaults.dynamodb.pointInTimeRecoveryEnabled))
              message: >-
                spec.mandatory.dynamodb.pointInTimeRecoveryEnabled and
                spec.defaults.dynamodb.pointInTimeRecoveryEnabled cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.dynamodb) &&
                  has(self.spec.mandatory.dynamodb.kmsMasterKeyId) &&
                  self.spec.mandatory.dynamodb.kmsMasterKeyId != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.dynamodb) &&
                  has(self.spec.defaults.dynamodb.kmsMasterKeyId) &&
                  self.spec.defaults.dynamodb.kmsMasterKeyId != "")
              message: >-
                spec.mandatory.dynamodb.kmsMasterKeyId and
                spec.defaults.dynamodb.kmsMasterKeyId cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.dynamodb) &&
                  has(self.spec.mandatory.dynamodb.billingMode) &&
                  self.spec.mandatory.dynamodb.billingMode != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.dynamodb) &&
                  has(self.spec.defaults.dynamodb.billingMode) &&
                  self.spec.defaults.dynamodb.billingMode != "")
              message: >-
                spec.mandatory.dynamodb.billingMode and
                spec.defaults.dynamodb.billingMode cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.dynamodb) &&
                  has(self.spec.mandatory.dynamodb.tableClass) &&
                  self.spec.mandatory.dynamodb.tableClass != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.dynamodb) &&
                  has(self.spec.defaults.dynamodb.tableClass) &&
                  self.spec.defaults.dynamodb.tableClass != "")
              message: >-
                spec.mandatory.dynamodb.tableClass and
                spec.defaults.dynamodb.tableClass cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.dynamodb) &&
                  has(self.spec.mandatory.dynamodb.contributorInsights) &&
                  self.spec.mandatory.dynamodb.contributorInsights != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.dynamodb) &&
                  has(self.spec.defaults.dynamodb.contributorInsights) &&
                  self.spec.defaults.dynamodb.contributorInsights != "")
              message: >-
                spec.mandatory.dynamodb.contributorInsights and
                spec.defaults.dynamodb.contributorInsights cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.eventbridge) &&
                  has(self.spec.mandatory.eventbridge.archiveRetentionDays) &&
                  self.spec.mandatory.eventbridge.archiveRetentionDays > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.eventbridge) &&
                  has(self.spec.defaults.eventbridge.archiveRetentionDays) &&
                  self.spec.defaults.eventbridge.archiveRetentionDays > 0)
              message: >-
                spec.mandatory.eventbridge.archiveRetentionDays and
                spec.defaults.eventbridge.archiveRetentionDays cannot both be non-zero.
                Set the field in exactly one tier.
            # CloudWatch Logs field mutual-exclusivity: a field must not appear in both
            # mandatory.cloudwatchlogs and defaults.cloudwatchlogs simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudwatchlogs) &&
                  has(self.spec.mandatory.cloudwatchlogs.kmsKeyId) &&
                  self.spec.mandatory.cloudwatchlogs.kmsKeyId != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudwatchlogs) &&
                  has(self.spec.defaults.cloudwatchlogs.kmsKeyId) &&
                  self.spec.defaults.cloudwatchlogs.kmsKeyId != "")
              message: >-
                spec.mandatory.cloudwatchlogs.kmsKeyId and
                spec.defaults.cloudwatchlogs.kmsKeyId cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudwatchlogs) &&
                  has(self.spec.mandatory.cloudwatchlogs.retentionDays) &&
                  self.spec.mandatory.cloudwatchlogs.retentionDays != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudwatchlogs) &&
                  has(self.spec.defaults.cloudwatchlogs.retentionDays) &&
                  self.spec.defaults.cloudwatchlogs.retentionDays != 0)
              message: >-
                spec.mandatory.cloudwatchlogs.retentionDays and
                spec.defaults.cloudwatchlogs.retentionDays cannot both be non-zero.
                Set the field in exactly one tier.
            # CloudTrail field mutual-exclusivity: a field must not appear in both
            # mandatory.cloudtrail and defaults.cloudtrail simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudtrail) &&
                  has(self.spec.mandatory.cloudtrail.isMultiRegionTrail) &&
                  self.spec.mandatory.cloudtrail.isMultiRegionTrail == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudtrail) &&
                  has(self.spec.defaults.cloudtrail.isMultiRegionTrail) &&
                  self.spec.defaults.cloudtrail.isMultiRegionTrail == true)
              message: >-
                spec.mandatory.cloudtrail.isMultiRegionTrail and
                spec.defaults.cloudtrail.isMultiRegionTrail cannot both be set to true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudtrail) &&
                  has(self.spec.mandatory.cloudtrail.enableLogFileValidation) &&
                  self.spec.mandatory.cloudtrail.enableLogFileValidation == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudtrail) &&
                  has(self.spec.defaults.cloudtrail.enableLogFileValidation) &&
                  self.spec.defaults.cloudtrail.enableLogFileValidation == true)
              message: >-
                spec.mandatory.cloudtrail.enableLogFileValidation and
                spec.defaults.cloudtrail.enableLogFileValidation cannot both be set to true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudtrail) &&
                  has(self.spec.mandatory.cloudtrail.retentionPeriod) &&
                  self.spec.mandatory.cloudtrail.retentionPeriod != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudtrail) &&
                  has(self.spec.defaults.cloudtrail.retentionPeriod) &&
                  self.spec.defaults.cloudtrail.retentionPeriod != 0)
              message: >-
                spec.mandatory.cloudtrail.retentionPeriod and
                spec.defaults.cloudtrail.retentionPeriod cannot both be non-zero.
                Set the field in exactly one tier.
            # CloudWatch field mutual-exclusivity: a field must not appear in both
            # mandatory.cloudwatch and defaults.cloudwatch simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudwatch) &&
                  has(self.spec.mandatory.cloudwatch.actionsEnabled) &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudwatch) &&
                  has(self.spec.defaults.cloudwatch.actionsEnabled))
              message: >-
                spec.mandatory.cloudwatch.actionsEnabled and
                spec.defaults.cloudwatch.actionsEnabled cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudwatch) &&
                  has(self.spec.mandatory.cloudwatch.treatMissingData) &&
                  self.spec.mandatory.cloudwatch.treatMissingData != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudwatch) &&
                  has(self.spec.defaults.cloudwatch.treatMissingData) &&
                  self.spec.defaults.cloudwatch.treatMissingData != "")
              message: >-
                spec.mandatory.cloudwatch.treatMissingData and
                spec.defaults.cloudwatch.treatMissingData cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudwatch) &&
                  has(self.spec.mandatory.cloudwatch.outputFormat) &&
                  self.spec.mandatory.cloudwatch.outputFormat != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudwatch) &&
                  has(self.spec.defaults.cloudwatch.outputFormat) &&
                  self.spec.defaults.cloudwatch.outputFormat != "")
              message: >-
                spec.mandatory.cloudwatch.outputFormat and
                spec.defaults.cloudwatch.outputFormat cannot both be set.
                Set the field in exactly one tier.
            # CloudFront field mutual-exclusivity: a field must not appear in both
            # mandatory.cloudfront and defaults.cloudfront simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudfront) &&
                  has(self.spec.mandatory.cloudfront.viewerProtocolPolicy) &&
                  self.spec.mandatory.cloudfront.viewerProtocolPolicy != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudfront) &&
                  has(self.spec.defaults.cloudfront.viewerProtocolPolicy) &&
                  self.spec.defaults.cloudfront.viewerProtocolPolicy != "")
              message: >-
                spec.mandatory.cloudfront.viewerProtocolPolicy and
                spec.defaults.cloudfront.viewerProtocolPolicy cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudfront) &&
                  has(self.spec.mandatory.cloudfront.minimumProtocolVersion) &&
                  self.spec.mandatory.cloudfront.minimumProtocolVersion != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudfront) &&
                  has(self.spec.defaults.cloudfront.minimumProtocolVersion) &&
                  self.spec.defaults.cloudfront.minimumProtocolVersion != "")
              message: >-
                spec.mandatory.cloudfront.minimumProtocolVersion and
                spec.defaults.cloudfront.minimumProtocolVersion cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudfront) &&
                  has(self.spec.mandatory.cloudfront.webACLRequired) &&
                  self.spec.mandatory.cloudfront.webACLRequired == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudfront) &&
                  has(self.spec.defaults.cloudfront.webACLRequired) &&
                  self.spec.defaults.cloudfront.webACLRequired == true)
              message: >-
                spec.mandatory.cloudfront.webACLRequired and
                spec.defaults.cloudfront.webACLRequired cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cloudfront) &&
                  has(self.spec.mandatory.cloudfront.loggingEnabled) &&
                  self.spec.mandatory.cloudfront.loggingEnabled == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.cloudfront) &&
                  has(self.spec.defaults.cloudfront.loggingEnabled) &&
                  self.spec.defaults.cloudfront.loggingEnabled == true)
              message: >-
                spec.mandatory.cloudfront.loggingEnabled and
                spec.defaults.cloudfront.loggingEnabled cannot both be true.
                Set the field in exactly one tier.
            # ELB field mutual-exclusivity: a boolean field must not be set to true in
            # both mandatory.elb and defaults.elb simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.elb) &&
                  has(self.spec.mandatory.elb.deletionProtection) &&
                  self.spec.mandatory.elb.deletionProtection == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.elb) &&
                  has(self.spec.defaults.elb.deletionProtection) &&
                  self.spec.defaults.elb.deletionProtection == true)
              message: >-
                spec.mandatory.elb.deletionProtection and
                spec.defaults.elb.deletionProtection cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.elb) &&
                  has(self.spec.mandatory.elb.accessLogsEnabled) &&
                  self.spec.mandatory.elb.accessLogsEnabled == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.elb) &&
                  has(self.spec.defaults.elb.accessLogsEnabled) &&
                  self.spec.defaults.elb.accessLogsEnabled == true)
              message: >-
                spec.mandatory.elb.accessLogsEnabled and
                spec.defaults.elb.accessLogsEnabled cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.elb) &&
                  has(self.spec.mandatory.elb.internalOnly) &&
                  self.spec.mandatory.elb.internalOnly == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.elb) &&
                  has(self.spec.defaults.elb.internalOnly) &&
                  self.spec.defaults.elb.internalOnly == true)
              message: >-
                spec.mandatory.elb.internalOnly and
                spec.defaults.elb.internalOnly cannot both be true.
                Set the field in exactly one tier.
            # RDS field mutual-exclusivity: a field must not appear in both
            # mandatory.rds and defaults.rds simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.storageEncrypted) &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.storageEncrypted))
              message: >-
                spec.mandatory.rds.storageEncrypted and
                spec.defaults.rds.storageEncrypted cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.deletionProtection) &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.deletionProtection))
              message: >-
                spec.mandatory.rds.deletionProtection and
                spec.defaults.rds.deletionProtection cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.multiAZ) &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.multiAZ))
              message: >-
                spec.mandatory.rds.multiAZ and
                spec.defaults.rds.multiAZ cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.publiclyAccessible) &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.publiclyAccessible))
              message: >-
                spec.mandatory.rds.publiclyAccessible and
                spec.defaults.rds.publiclyAccessible cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.autoMinorVersionUpgrade) &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.autoMinorVersionUpgrade))
              message: >-
                spec.mandatory.rds.autoMinorVersionUpgrade and
                spec.defaults.rds.autoMinorVersionUpgrade cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.copyTagsToSnapshot) &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.copyTagsToSnapshot))
              message: >-
                spec.mandatory.rds.copyTagsToSnapshot and
                spec.defaults.rds.copyTagsToSnapshot cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.performanceInsightsEnabled) &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.performanceInsightsEnabled))
              message: >-
                spec.mandatory.rds.performanceInsightsEnabled and
                spec.defaults.rds.performanceInsightsEnabled cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.enableIAMDatabaseAuthentication) &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.enableIAMDatabaseAuthentication))
              message: >-
                spec.mandatory.rds.enableIAMDatabaseAuthentication and
                spec.defaults.rds.enableIAMDatabaseAuthentication cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.manageMasterUserPassword) &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.manageMasterUserPassword))
              message: >-
                spec.mandatory.rds.manageMasterUserPassword and
                spec.defaults.rds.manageMasterUserPassword cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.kmsKeyID) &&
                  self.spec.mandatory.rds.kmsKeyID != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.kmsKeyID) &&
                  self.spec.defaults.rds.kmsKeyID != "")
              message: >-
                spec.mandatory.rds.kmsKeyID and
                spec.defaults.rds.kmsKeyID cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.storageType) &&
                  self.spec.mandatory.rds.storageType != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.storageType) &&
                  self.spec.defaults.rds.storageType != "")
              message: >-
                spec.mandatory.rds.storageType and
                spec.defaults.rds.storageType cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.namingTemplate) &&
                  self.spec.mandatory.rds.namingTemplate != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.namingTemplate) &&
                  self.spec.defaults.rds.namingTemplate != "")
              message: >-
                spec.mandatory.rds.namingTemplate and
                spec.defaults.rds.namingTemplate cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.rds) &&
                  has(self.spec.mandatory.rds.backupRetentionPeriod) &&
                  self.spec.mandatory.rds.backupRetentionPeriod != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.rds) &&
                  has(self.spec.defaults.rds.backupRetentionPeriod) &&
                  self.spec.defaults.rds.backupRetentionPeriod != 0)
              message: >-
                spec.mandatory.rds.backupRetentionPeriod and
                spec.defaults.rds.backupRetentionPeriod cannot both be non-zero.
                Set the field in exactly one tier.
            # Auto Scaling field mutual-exclusivity (ADR-015 §3.5).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.autoscaling) &&
                  has(self.spec.mandatory.autoscaling.newInstancesProtectedFromScaleIn) &&
                  self.spec.mandatory.autoscaling.newInstancesProtectedFromScaleIn == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.autoscaling) &&
                  has(self.spec.defaults.autoscaling.newInstancesProtectedFromScaleIn) &&
                  self.spec.defaults.autoscaling.newInstancesProtectedFromScaleIn == true)
              message: >-
                spec.mandatory.autoscaling.newInstancesProtectedFromScaleIn and
                spec.defaults.autoscaling.newInstancesProtectedFromScaleIn cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.autoscaling) &&
                  has(self.spec.mandatory.autoscaling.capacityRebalance) &&
                  self.spec.mandatory.autoscaling.capacityRebalance == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.autoscaling) &&
                  has(self.spec.defaults.autoscaling.capacityRebalance) &&
                  self.spec.defaults.autoscaling.capacityRebalance == true)
              message: >-
                spec.mandatory.autoscaling.capacityRebalance and
                spec.defaults.autoscaling.capacityRebalance cannot both be true.
                Set the field in exactly one tier.
            # Lambda field mutual-exclusivity: a field must not appear in both
            # mandatory.lambda and defaults.lambda simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.lambda) &&
                  has(self.spec.mandatory.lambda.runtime) &&
                  self.spec.mandatory.lambda.runtime != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.lambda) &&
                  has(self.spec.defaults.lambda.runtime) &&
                  self.spec.defaults.lambda.runtime != "")
              message: >-
                spec.mandatory.lambda.runtime and
                spec.defaults.lambda.runtime cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.lambda) &&
                  has(self.spec.mandatory.lambda.memorySize) &&
                  self.spec.mandatory.lambda.memorySize > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.lambda) &&
                  has(self.spec.defaults.lambda.memorySize) &&
                  self.spec.defaults.lambda.memorySize > 0)
              message: >-
                spec.mandatory.lambda.memorySize and
                spec.defaults.lambda.memorySize cannot both be non-zero.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.lambda) &&
                  has(self.spec.mandatory.lambda.timeout) &&
                  self.spec.mandatory.lambda.timeout > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.lambda) &&
                  has(self.spec.defaults.lambda.timeout) &&
                  self.spec.defaults.lambda.timeout > 0)
              message: >-
                spec.mandatory.lambda.timeout and
                spec.defaults.lambda.timeout cannot both be non-zero.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.lambda) &&
                  has(self.spec.mandatory.lambda.tracingMode) &&
                  self.spec.mandatory.lambda.tracingMode != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.lambda) &&
                  has(self.spec.defaults.lambda.tracingMode) &&
                  self.spec.defaults.lambda.tracingMode != "")
              message: >-
                spec.mandatory.lambda.tracingMode and
                spec.defaults.lambda.tracingMode cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.lambda) &&
                  has(self.spec.mandatory.lambda.kmsKeyArn) &&
                  self.spec.mandatory.lambda.kmsKeyArn != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.lambda) &&
                  has(self.spec.defaults.lambda.kmsKeyArn) &&
                  self.spec.defaults.lambda.kmsKeyArn != "")
              message: >-
                spec.mandatory.lambda.kmsKeyArn and
                spec.defaults.lambda.kmsKeyArn cannot both be set.
                Set the field in exactly one tier.
            # EC2 field mutual-exclusivity: a boolean field must not be set to true in
            # both mandatory.ec2 and defaults.ec2 simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ec2) &&
                  has(self.spec.mandatory.ec2.flowLogsRequired) &&
                  self.spec.mandatory.ec2.flowLogsRequired == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.ec2) &&
                  has(self.spec.defaults.ec2.flowLogsRequired) &&
                  self.spec.defaults.ec2.flowLogsRequired == true)
              message: >-
                spec.mandatory.ec2.flowLogsRequired and
                spec.defaults.ec2.flowLogsRequired cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ec2) &&
                  has(self.spec.mandatory.ec2.imdsv2Required) &&
                  self.spec.mandatory.ec2.imdsv2Required == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.ec2) &&
                  has(self.spec.defaults.ec2.imdsv2Required) &&
                  self.spec.defaults.ec2.imdsv2Required == true)
              message: >-
                spec.mandatory.ec2.imdsv2Required and
                spec.defaults.ec2.imdsv2Required cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ec2) &&
                  has(self.spec.mandatory.ec2.ebsEncryptionRequired) &&
                  self.spec.mandatory.ec2.ebsEncryptionRequired == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.ec2) &&
                  has(self.spec.defaults.ec2.ebsEncryptionRequired) &&
                  self.spec.defaults.ec2.ebsEncryptionRequired == true)
              message: >-
                spec.mandatory.ec2.ebsEncryptionRequired and
                spec.defaults.ec2.ebsEncryptionRequired cannot both be true.
                Set the field in exactly one tier.
            # ECS field mutual-exclusivity: a field must not appear in both
            # mandatory.ecs and defaults.ecs simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ecs) &&
                  has(self.spec.mandatory.ecs.containerInsights) &&
                  self.spec.mandatory.ecs.containerInsights == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.ecs) &&
                  has(self.spec.defaults.ecs.containerInsights) &&
                  self.spec.defaults.ecs.containerInsights == true)
              message: >-
                spec.mandatory.ecs.containerInsights and
                spec.defaults.ecs.containerInsights cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ecs) &&
                  has(self.spec.mandatory.ecs.defaultLaunchType) &&
                  self.spec.mandatory.ecs.defaultLaunchType != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.ecs) &&
                  has(self.spec.defaults.ecs.defaultLaunchType) &&
                  self.spec.defaults.ecs.defaultLaunchType != "")
              message: >-
                spec.mandatory.ecs.defaultLaunchType and
                spec.defaults.ecs.defaultLaunchType cannot both be set.
                Set the field in exactly one tier.
            # API Gateway V2 field mutual-exclusivity: a field must not appear in both
            # mandatory.apigatewayv2 and defaults.apigatewayv2 simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.apigatewayv2) &&
                  has(self.spec.mandatory.apigatewayv2.corsEnabled) &&
                  self.spec.mandatory.apigatewayv2.corsEnabled == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.apigatewayv2) &&
                  has(self.spec.defaults.apigatewayv2.corsEnabled) &&
                  self.spec.defaults.apigatewayv2.corsEnabled == true)
              message: >-
                spec.mandatory.apigatewayv2.corsEnabled and
                spec.defaults.apigatewayv2.corsEnabled cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.apigatewayv2) &&
                  has(self.spec.mandatory.apigatewayv2.disableExecuteApiEndpoint) &&
                  self.spec.mandatory.apigatewayv2.disableExecuteApiEndpoint == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.apigatewayv2) &&
                  has(self.spec.defaults.apigatewayv2.disableExecuteApiEndpoint) &&
                  self.spec.defaults.apigatewayv2.disableExecuteApiEndpoint == true)
              message: >-
                spec.mandatory.apigatewayv2.disableExecuteApiEndpoint and
                spec.defaults.apigatewayv2.disableExecuteApiEndpoint cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.apigatewayv2) &&
                  has(self.spec.mandatory.apigatewayv2.minimumTlsVersion) &&
                  self.spec.mandatory.apigatewayv2.minimumTlsVersion != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.apigatewayv2) &&
                  has(self.spec.defaults.apigatewayv2.minimumTlsVersion) &&
                  self.spec.defaults.apigatewayv2.minimumTlsVersion != "")
              message: >-
                spec.mandatory.apigatewayv2.minimumTlsVersion and
                spec.defaults.apigatewayv2.minimumTlsVersion cannot both be set.
                Set the field in exactly one tier.
            # ElastiCache field mutual-exclusivity: a boolean field must not be set to true in
            # both mandatory.elasticache and defaults.elasticache simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.elasticache) &&
                  has(self.spec.mandatory.elasticache.atRestEncryptionEnabled) &&
                  self.spec.mandatory.elasticache.atRestEncryptionEnabled == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.elasticache) &&
                  has(self.spec.defaults.elasticache.atRestEncryptionEnabled) &&
                  self.spec.defaults.elasticache.atRestEncryptionEnabled == true)
              message: >-
                spec.mandatory.elasticache.atRestEncryptionEnabled and
                spec.defaults.elasticache.atRestEncryptionEnabled cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.elasticache) &&
                  has(self.spec.mandatory.elasticache.transitEncryptionEnabled) &&
                  self.spec.mandatory.elasticache.transitEncryptionEnabled == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.elasticache) &&
                  has(self.spec.defaults.elasticache.transitEncryptionEnabled) &&
                  self.spec.defaults.elasticache.transitEncryptionEnabled == true)
              message: >-
                spec.mandatory.elasticache.transitEncryptionEnabled and
                spec.defaults.elasticache.transitEncryptionEnabled cannot both be true.
                Set the field in exactly one tier.
            # ECR field mutual-exclusivity: a field must not appear in both
            # mandatory.ecr and defaults.ecr simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ecr) &&
                  has(self.spec.mandatory.ecr.imageTagMutability) &&
                  self.spec.mandatory.ecr.imageTagMutability != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.ecr) &&
                  has(self.spec.defaults.ecr.imageTagMutability) &&
                  self.spec.defaults.ecr.imageTagMutability != "")
              message: >-
                spec.mandatory.ecr.imageTagMutability and
                spec.defaults.ecr.imageTagMutability cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ecr) &&
                  has(self.spec.mandatory.ecr.encryptionType) &&
                  self.spec.mandatory.ecr.encryptionType != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.ecr) &&
                  has(self.spec.defaults.ecr.encryptionType) &&
                  self.spec.defaults.ecr.encryptionType != "")
              message: >-
                spec.mandatory.ecr.encryptionType and
                spec.defaults.ecr.encryptionType cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.ecr) &&
                  has(self.spec.mandatory.ecr.lifecyclePolicy) &&
                  self.spec.mandatory.ecr.lifecyclePolicy != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.ecr) &&
                  has(self.spec.defaults.ecr.lifecyclePolicy) &&
                  self.spec.defaults.ecr.lifecyclePolicy != "")
              message: >-
                spec.mandatory.ecr.lifecyclePolicy and
                spec.defaults.ecr.lifecyclePolicy cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.stepfunctions) &&
                  has(self.spec.mandatory.stepfunctions.loggingLevel) &&
                  self.spec.mandatory.stepfunctions.loggingLevel != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.stepfunctions) &&
                  has(self.spec.defaults.stepfunctions.loggingLevel) &&
                  self.spec.defaults.stepfunctions.loggingLevel != "")
              message: >-
                spec.mandatory.stepfunctions.loggingLevel and
                spec.defaults.stepfunctions.loggingLevel cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.msk) &&
                  has(self.spec.mandatory.msk.kafkaVersion) &&
                  self.spec.mandatory.msk.kafkaVersion != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.msk) &&
                  has(self.spec.defaults.msk.kafkaVersion) &&
                  self.spec.defaults.msk.kafkaVersion != "")
              message: >-
                spec.mandatory.msk.kafkaVersion and
                spec.defaults.msk.kafkaVersion cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.msk) &&
                  has(self.spec.mandatory.msk.enhancedMonitoring) &&
                  self.spec.mandatory.msk.enhancedMonitoring != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.msk) &&
                  has(self.spec.defaults.msk.enhancedMonitoring) &&
                  self.spec.defaults.msk.enhancedMonitoring != "")
              message: >-
                spec.mandatory.msk.enhancedMonitoring and
                spec.defaults.msk.enhancedMonitoring cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.msk) &&
                  has(self.spec.mandatory.msk.encryptionInTransitClientBroker) &&
                  self.spec.mandatory.msk.encryptionInTransitClientBroker != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.msk) &&
                  has(self.spec.defaults.msk.encryptionInTransitClientBroker) &&
                  self.spec.defaults.msk.encryptionInTransitClientBroker != "")
              message: >-
                spec.mandatory.msk.encryptionInTransitClientBroker and
                spec.defaults.msk.encryptionInTransitClientBroker cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.msk) &&
                  has(self.spec.mandatory.msk.encryptionInTransitInCluster) &&
                  self.spec.mandatory.msk.encryptionInTransitInCluster != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.msk) &&
                  has(self.spec.defaults.msk.encryptionInTransitInCluster) &&
                  self.spec.defaults.msk.encryptionInTransitInCluster != "")
              message: >-
                spec.mandatory.msk.encryptionInTransitInCluster and
                spec.defaults.msk.encryptionInTransitInCluster cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.msk) &&
                  has(self.spec.mandatory.msk.encryptionAtRestKmsKeyId) &&
                  self.spec.mandatory.msk.encryptionAtRestKmsKeyId != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.msk) &&
                  has(self.spec.defaults.msk.encryptionAtRestKmsKeyId) &&
                  self.spec.defaults.msk.encryptionAtRestKmsKeyId != "")
              message: >-
                spec.mandatory.msk.encryptionAtRestKmsKeyId and
                spec.defaults.msk.encryptionAtRestKmsKeyId cannot both be set.
                Set the field in exactly one tier.
            # MemoryDB field mutual-exclusivity (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.memorydb) &&
                  has(self.spec.mandatory.memorydb.tlsEnabled) &&
                  self.spec.mandatory.memorydb.tlsEnabled == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.memorydb) &&
                  has(self.spec.defaults.memorydb.tlsEnabled) &&
                  self.spec.defaults.memorydb.tlsEnabled == true)
              message: >-
                spec.mandatory.memorydb.tlsEnabled and
                spec.defaults.memorydb.tlsEnabled cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.memorydb) &&
                  has(self.spec.mandatory.memorydb.allowedNodeTypes) &&
                  size(self.spec.mandatory.memorydb.allowedNodeTypes) > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.memorydb) &&
                  has(self.spec.defaults.memorydb.allowedNodeTypes) &&
                  size(self.spec.defaults.memorydb.allowedNodeTypes) > 0)
              message: >-
                spec.mandatory.memorydb.allowedNodeTypes and
                spec.defaults.memorydb.allowedNodeTypes cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.memorydb) &&
                  has(self.spec.mandatory.memorydb.snapshotRetentionLimit) &&
                  self.spec.mandatory.memorydb.snapshotRetentionLimit > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.memorydb) &&
                  has(self.spec.defaults.memorydb.snapshotRetentionLimit) &&
                  self.spec.defaults.memorydb.snapshotRetentionLimit > 0)
              message: >-
                spec.mandatory.memorydb.snapshotRetentionLimit and
                spec.defaults.memorydb.snapshotRetentionLimit cannot both be set.
                Set the field in exactly one tier.
            # MQ field mutual-exclusivity (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mq) &&
                  has(self.spec.mandatory.mq.engineType) &&
                  self.spec.mandatory.mq.engineType != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.mq) &&
                  has(self.spec.defaults.mq.engineType) &&
                  self.spec.defaults.mq.engineType != "")
              message: >-
                spec.mandatory.mq.engineType and
                spec.defaults.mq.engineType cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mq) &&
                  has(self.spec.mandatory.mq.authenticationStrategy) &&
                  self.spec.mandatory.mq.authenticationStrategy != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.mq) &&
                  has(self.spec.defaults.mq.authenticationStrategy) &&
                  self.spec.defaults.mq.authenticationStrategy != "")
              message: >-
                spec.mandatory.mq.authenticationStrategy and
                spec.defaults.mq.authenticationStrategy cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mq) &&
                  has(self.spec.mandatory.mq.deploymentMode) &&
                  self.spec.mandatory.mq.deploymentMode != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.mq) &&
                  has(self.spec.defaults.mq.deploymentMode) &&
                  self.spec.defaults.mq.deploymentMode != "")
              message: >-
                spec.mandatory.mq.deploymentMode and
                spec.defaults.mq.deploymentMode cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mq) &&
                  has(self.spec.mandatory.mq.storageType) &&
                  self.spec.mandatory.mq.storageType != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.mq) &&
                  has(self.spec.defaults.mq.storageType) &&
                  self.spec.defaults.mq.storageType != "")
              message: >-
                spec.mandatory.mq.storageType and
                spec.defaults.mq.storageType cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mq) &&
                  has(self.spec.mandatory.mq.publiclyAccessible) &&
                  self.spec.mandatory.mq.publiclyAccessible == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.mq) &&
                  has(self.spec.defaults.mq.publiclyAccessible) &&
                  self.spec.defaults.mq.publiclyAccessible == true)
              message: >-
                spec.mandatory.mq.publiclyAccessible and
                spec.defaults.mq.publiclyAccessible cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mq) &&
                  has(self.spec.mandatory.mq.autoMinorVersionUpgrade) &&
                  self.spec.mandatory.mq.autoMinorVersionUpgrade == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.mq) &&
                  has(self.spec.defaults.mq.autoMinorVersionUpgrade) &&
                  self.spec.defaults.mq.autoMinorVersionUpgrade == true)
              message: >-
                spec.mandatory.mq.autoMinorVersionUpgrade and
                spec.defaults.mq.autoMinorVersionUpgrade cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mq) &&
                  has(self.spec.mandatory.mq.logsGeneral) &&
                  self.spec.mandatory.mq.logsGeneral == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.mq) &&
                  has(self.spec.defaults.mq.logsGeneral) &&
                  self.spec.defaults.mq.logsGeneral == true)
              message: >-
                spec.mandatory.mq.logsGeneral and
                spec.defaults.mq.logsGeneral cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mq) &&
                  has(self.spec.mandatory.mq.logsAudit) &&
                  self.spec.mandatory.mq.logsAudit == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.mq) &&
                  has(self.spec.defaults.mq.logsAudit) &&
                  self.spec.defaults.mq.logsAudit == true)
              message: >-
                spec.mandatory.mq.logsAudit and
                spec.defaults.mq.logsAudit cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mq) &&
                  has(self.spec.mandatory.mq.encryptionUseAWSOwnedKey) &&
                  self.spec.mandatory.mq.encryptionUseAWSOwnedKey == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.mq) &&
                  has(self.spec.defaults.mq.encryptionUseAWSOwnedKey) &&
                  self.spec.defaults.mq.encryptionUseAWSOwnedKey == true)
              message: >-
                spec.mandatory.mq.encryptionUseAWSOwnedKey and
                spec.defaults.mq.encryptionUseAWSOwnedKey cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.certificateManager) &&
                  has(self.spec.mandatory.certificateManager.keyAlgorithm) &&
                  self.spec.mandatory.certificateManager.keyAlgorithm != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.certificateManager) &&
                  has(self.spec.defaults.certificateManager.keyAlgorithm) &&
                  self.spec.defaults.certificateManager.keyAlgorithm != "")
              message: >-
                spec.mandatory.certificateManager.keyAlgorithm and
                spec.defaults.certificateManager.keyAlgorithm cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.certificateManager) &&
                  has(self.spec.mandatory.certificateManager.certificateTransparencyLogging) &&
                  self.spec.mandatory.certificateManager.certificateTransparencyLogging != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.certificateManager) &&
                  has(self.spec.defaults.certificateManager.certificateTransparencyLogging) &&
                  self.spec.defaults.certificateManager.certificateTransparencyLogging != "")
              message: >-
                spec.mandatory.certificateManager.certificateTransparencyLogging and
                spec.defaults.certificateManager.certificateTransparencyLogging cannot both be set.
                Set the field in exactly one tier.
            # DocumentDB field mutual-exclusivity (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.documentdb) &&
                  has(self.spec.mandatory.documentdb.storageEncrypted) &&
                  self.spec.mandatory.documentdb.storageEncrypted == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.documentdb) &&
                  has(self.spec.defaults.documentdb.storageEncrypted) &&
                  self.spec.defaults.documentdb.storageEncrypted == true)
              message: >-
                spec.mandatory.documentdb.storageEncrypted and
                spec.defaults.documentdb.storageEncrypted cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.documentdb) &&
                  has(self.spec.mandatory.documentdb.deletionProtection) &&
                  self.spec.mandatory.documentdb.deletionProtection == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.documentdb) &&
                  has(self.spec.defaults.documentdb.deletionProtection) &&
                  self.spec.defaults.documentdb.deletionProtection == true)
              message: >-
                spec.mandatory.documentdb.deletionProtection and
                spec.defaults.documentdb.deletionProtection cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.documentdb) &&
                  has(self.spec.mandatory.documentdb.allowedInstanceClasses) &&
                  size(self.spec.mandatory.documentdb.allowedInstanceClasses) > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.documentdb) &&
                  has(self.spec.defaults.documentdb.allowedInstanceClasses) &&
                  size(self.spec.defaults.documentdb.allowedInstanceClasses) > 0)
              message: >-
                spec.mandatory.documentdb.allowedInstanceClasses and
                spec.defaults.documentdb.allowedInstanceClasses cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.documentdb) &&
                  has(self.spec.mandatory.documentdb.backupRetentionPeriod) &&
                  self.spec.mandatory.documentdb.backupRetentionPeriod > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.documentdb) &&
                  has(self.spec.defaults.documentdb.backupRetentionPeriod) &&
                  self.spec.defaults.documentdb.backupRetentionPeriod > 0)
              message: >-
                spec.mandatory.documentdb.backupRetentionPeriod and
                spec.defaults.documentdb.backupRetentionPeriod cannot both be set.
                Set the field in exactly one tier.
            # OpenSearch field mutual-exclusivity (ADR-015 §3.2). 4 KropathConfig-scoped fields.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.opensearch) &&
                  has(self.spec.mandatory.opensearch.encryptionAtRestEnabled) &&
                  self.spec.mandatory.opensearch.encryptionAtRestEnabled == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.opensearch) &&
                  has(self.spec.defaults.opensearch.encryptionAtRestEnabled) &&
                  self.spec.defaults.opensearch.encryptionAtRestEnabled == true)
              message: >-
                spec.mandatory.opensearch.encryptionAtRestEnabled and
                spec.defaults.opensearch.encryptionAtRestEnabled cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.opensearch) &&
                  has(self.spec.mandatory.opensearch.nodeToNodeEncryptionEnabled) &&
                  self.spec.mandatory.opensearch.nodeToNodeEncryptionEnabled == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.opensearch) &&
                  has(self.spec.defaults.opensearch.nodeToNodeEncryptionEnabled) &&
                  self.spec.defaults.opensearch.nodeToNodeEncryptionEnabled == true)
              message: >-
                spec.mandatory.opensearch.nodeToNodeEncryptionEnabled and
                spec.defaults.opensearch.nodeToNodeEncryptionEnabled cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.opensearch) &&
                  has(self.spec.mandatory.opensearch.enforceHTTPS) &&
                  self.spec.mandatory.opensearch.enforceHTTPS == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.opensearch) &&
                  has(self.spec.defaults.opensearch.enforceHTTPS) &&
                  self.spec.defaults.opensearch.enforceHTTPS == true)
              message: >-
                spec.mandatory.opensearch.enforceHTTPS and
                spec.defaults.opensearch.enforceHTTPS cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.opensearch) &&
                  has(self.spec.mandatory.opensearch.tlsSecurityPolicy) &&
                  self.spec.mandatory.opensearch.tlsSecurityPolicy != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.opensearch) &&
                  has(self.spec.defaults.opensearch.tlsSecurityPolicy) &&
                  self.spec.defaults.opensearch.tlsSecurityPolicy != "")
              message: >-
                spec.mandatory.opensearch.tlsSecurityPolicy and
                spec.defaults.opensearch.tlsSecurityPolicy cannot both be set.
                Set the field in exactly one tier.
            # EventBridge Pipes field mutual-exclusivity (ADR-015 §3.5).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.pipes) &&
                  has(self.spec.mandatory.pipes.desiredState) &&
                  self.spec.mandatory.pipes.desiredState != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.pipes) &&
                  has(self.spec.defaults.pipes.desiredState) &&
                  self.spec.defaults.pipes.desiredState != "")
              message: >-
                spec.mandatory.pipes.desiredState and spec.defaults.pipes.desiredState
                cannot both be set. Set the field in exactly one tier.
            # Aurora DSQL field mutual-exclusivity (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.dsql) &&
                  has(self.spec.mandatory.dsql.deletionProtectionEnabled) &&
                  self.spec.mandatory.dsql.deletionProtectionEnabled == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.dsql) &&
                  has(self.spec.defaults.dsql.deletionProtectionEnabled) &&
                  self.spec.defaults.dsql.deletionProtectionEnabled == true)
              message: >-
                spec.mandatory.dsql.deletionProtectionEnabled and
                spec.defaults.dsql.deletionProtectionEnabled cannot both be true.
                Set the field in exactly one tier.
            # Route53 field mutual-exclusivity (ADR-015 §3.5).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.route53) &&
                  has(self.spec.mandatory.route53.defaultTTL) &&
                  self.spec.mandatory.route53.defaultTTL > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.route53) &&
                  has(self.spec.defaults.route53.defaultTTL) &&
                  self.spec.defaults.route53.defaultTTL > 0)
              message: >-
                spec.mandatory.route53.defaultTTL and spec.defaults.route53.defaultTTL
                cannot both be non-zero. Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.route53) &&
                  has(self.spec.mandatory.route53.healthCheckRequestInterval) &&
                  self.spec.mandatory.route53.healthCheckRequestInterval > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.route53) &&
                  has(self.spec.defaults.route53.healthCheckRequestInterval) &&
                  self.spec.defaults.route53.healthCheckRequestInterval > 0)
              message: >-
                spec.mandatory.route53.healthCheckRequestInterval and
                spec.defaults.route53.healthCheckRequestInterval cannot both be non-zero.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.route53) &&
                  has(self.spec.mandatory.route53.healthCheckFailureThreshold) &&
                  self.spec.mandatory.route53.healthCheckFailureThreshold > 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.route53) &&
                  has(self.spec.defaults.route53.healthCheckFailureThreshold) &&
                  self.spec.defaults.route53.healthCheckFailureThreshold > 0)
              message: >-
                spec.mandatory.route53.healthCheckFailureThreshold and
                spec.defaults.route53.healthCheckFailureThreshold cannot both be non-zero.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.route53) &&
                  has(self.spec.mandatory.route53.resolverEndpointType) &&
                  self.spec.mandatory.route53.resolverEndpointType != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.route53) &&
                  has(self.spec.defaults.route53.resolverEndpointType) &&
                  self.spec.defaults.route53.resolverEndpointType != "")
              message: >-
                spec.mandatory.route53.resolverEndpointType and
                spec.defaults.route53.resolverEndpointType cannot both be non-empty.
                Set the field in exactly one tier.
            # Cognito field mutual-exclusivity: a field must not appear in both
            # mandatory.cognito and defaults.cognito simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cognito) &&
                  has(self.spec.mandatory.cognito.mfaConfiguration) &&
                  self.spec.mandatory.cognito.mfaConfiguration != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.cognito) &&
                  has(self.spec.defaults.cognito.mfaConfiguration) &&
                  self.spec.defaults.cognito.mfaConfiguration != "")
              message: >-
                spec.mandatory.cognito.mfaConfiguration and
                spec.defaults.cognito.mfaConfiguration cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cognito) &&
                  has(self.spec.mandatory.cognito.deletionProtection) &&
                  self.spec.mandatory.cognito.deletionProtection != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.cognito) &&
                  has(self.spec.defaults.cognito.deletionProtection) &&
                  self.spec.defaults.cognito.deletionProtection != "")
              message: >-
                spec.mandatory.cognito.deletionProtection and
                spec.defaults.cognito.deletionProtection cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cognito) &&
                  has(self.spec.mandatory.cognito.advancedSecurityMode) &&
                  self.spec.mandatory.cognito.advancedSecurityMode != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.cognito) &&
                  has(self.spec.defaults.cognito.advancedSecurityMode) &&
                  self.spec.defaults.cognito.advancedSecurityMode != "")
              message: >-
                spec.mandatory.cognito.advancedSecurityMode and
                spec.defaults.cognito.advancedSecurityMode cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cognito) &&
                  has(self.spec.mandatory.cognito.passwordPolicy) &&
                  has(self.spec.mandatory.cognito.passwordPolicy.minimumLength) &&
                  self.spec.mandatory.cognito.passwordPolicy.minimumLength != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.cognito) &&
                  has(self.spec.defaults.cognito.passwordPolicy) &&
                  has(self.spec.defaults.cognito.passwordPolicy.minimumLength) &&
                  self.spec.defaults.cognito.passwordPolicy.minimumLength != 0)
              message: >-
                spec.mandatory.cognito.passwordPolicy.minimumLength and
                spec.defaults.cognito.passwordPolicy.minimumLength cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cognito) &&
                  has(self.spec.mandatory.cognito.passwordPolicy) &&
                  has(self.spec.mandatory.cognito.passwordPolicy.temporaryPasswordValidityDays) &&
                  self.spec.mandatory.cognito.passwordPolicy.temporaryPasswordValidityDays != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.cognito) &&
                  has(self.spec.defaults.cognito.passwordPolicy) &&
                  has(self.spec.defaults.cognito.passwordPolicy.temporaryPasswordValidityDays) &&
                  self.spec.defaults.cognito.passwordPolicy.temporaryPasswordValidityDays != 0)
              message: >-
                spec.mandatory.cognito.passwordPolicy.temporaryPasswordValidityDays and
                spec.defaults.cognito.passwordPolicy.temporaryPasswordValidityDays cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cognito) &&
                  has(self.spec.mandatory.cognito.passwordPolicy) &&
                  has(self.spec.mandatory.cognito.passwordPolicy.requireLowercase) &&
                  self.spec.mandatory.cognito.passwordPolicy.requireLowercase == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.cognito) &&
                  has(self.spec.defaults.cognito.passwordPolicy) &&
                  has(self.spec.defaults.cognito.passwordPolicy.requireLowercase) &&
                  self.spec.defaults.cognito.passwordPolicy.requireLowercase == true)
              message: >-
                spec.mandatory.cognito.passwordPolicy.requireLowercase and
                spec.defaults.cognito.passwordPolicy.requireLowercase cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cognito) &&
                  has(self.spec.mandatory.cognito.passwordPolicy) &&
                  has(self.spec.mandatory.cognito.passwordPolicy.requireNumbers) &&
                  self.spec.mandatory.cognito.passwordPolicy.requireNumbers == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.cognito) &&
                  has(self.spec.defaults.cognito.passwordPolicy) &&
                  has(self.spec.defaults.cognito.passwordPolicy.requireNumbers) &&
                  self.spec.defaults.cognito.passwordPolicy.requireNumbers == true)
              message: >-
                spec.mandatory.cognito.passwordPolicy.requireNumbers and
                spec.defaults.cognito.passwordPolicy.requireNumbers cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cognito) &&
                  has(self.spec.mandatory.cognito.passwordPolicy) &&
                  has(self.spec.mandatory.cognito.passwordPolicy.requireSymbols) &&
                  self.spec.mandatory.cognito.passwordPolicy.requireSymbols == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.cognito) &&
                  has(self.spec.defaults.cognito.passwordPolicy) &&
                  has(self.spec.defaults.cognito.passwordPolicy.requireSymbols) &&
                  self.spec.defaults.cognito.passwordPolicy.requireSymbols == true)
              message: >-
                spec.mandatory.cognito.passwordPolicy.requireSymbols and
                spec.defaults.cognito.passwordPolicy.requireSymbols cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.cognito) &&
                  has(self.spec.mandatory.cognito.passwordPolicy) &&
                  has(self.spec.mandatory.cognito.passwordPolicy.requireUppercase) &&
                  self.spec.mandatory.cognito.passwordPolicy.requireUppercase == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.cognito) &&
                  has(self.spec.defaults.cognito.passwordPolicy) &&
                  has(self.spec.defaults.cognito.passwordPolicy.requireUppercase) &&
                  self.spec.defaults.cognito.passwordPolicy.requireUppercase == true)
              message: >-
                spec.mandatory.cognito.passwordPolicy.requireUppercase and
                spec.defaults.cognito.passwordPolicy.requireUppercase cannot both be true.
                Set the field in exactly one tier.
            # Keyspaces field mutual-exclusivity: a field must not appear in both
            # mandatory.keyspaces and defaults.keyspaces simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.keyspaces) &&
                  has(self.spec.mandatory.keyspaces.replicationStrategy) &&
                  self.spec.mandatory.keyspaces.replicationStrategy != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.keyspaces) &&
                  has(self.spec.defaults.keyspaces.replicationStrategy) &&
                  self.spec.defaults.keyspaces.replicationStrategy != "")
              message: >-
                spec.mandatory.keyspaces.replicationStrategy and
                spec.defaults.keyspaces.replicationStrategy cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.keyspaces) &&
                  has(self.spec.mandatory.keyspaces.throughputMode) &&
                  self.spec.mandatory.keyspaces.throughputMode != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.keyspaces) &&
                  has(self.spec.defaults.keyspaces.throughputMode) &&
                  self.spec.defaults.keyspaces.throughputMode != "")
              message: >-
                spec.mandatory.keyspaces.throughputMode and
                spec.defaults.keyspaces.throughputMode cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.keyspaces) &&
                  has(self.spec.mandatory.keyspaces.encryptionType) &&
                  self.spec.mandatory.keyspaces.encryptionType != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.keyspaces) &&
                  has(self.spec.defaults.keyspaces.encryptionType) &&
                  self.spec.defaults.keyspaces.encryptionType != "")
              message: >-
                spec.mandatory.keyspaces.encryptionType and
                spec.defaults.keyspaces.encryptionType cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.keyspaces) &&
                  self.spec.mandatory.keyspaces.pointInTimeRecovery == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.keyspaces) &&
                  self.spec.defaults.keyspaces.pointInTimeRecovery == true)
              message: >-
                spec.mandatory.keyspaces.pointInTimeRecovery and
                spec.defaults.keyspaces.pointInTimeRecovery cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.keyspaces) &&
                  self.spec.mandatory.keyspaces.ttlEnabled == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.keyspaces) &&
                  self.spec.defaults.keyspaces.ttlEnabled == true)
              message: >-
                spec.mandatory.keyspaces.ttlEnabled and
                spec.defaults.keyspaces.ttlEnabled cannot both be true.
                Set the field in exactly one tier.
            # Bedrock field mutual-exclusivity: a field must not appear in both
            # mandatory.bedrock and defaults.bedrock simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.bedrock) &&
                  has(self.spec.mandatory.bedrock.guardrailIdentifier) &&
                  has(self.spec.defaults) && has(self.spec.defaults.bedrock) &&
                  has(self.spec.defaults.bedrock.guardrailIdentifier))
              message: >-
                spec.mandatory.bedrock.guardrailIdentifier and
                spec.defaults.bedrock.guardrailIdentifier cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.bedrock) &&
                  has(self.spec.mandatory.bedrock.guardrailVersion) &&
                  has(self.spec.defaults) && has(self.spec.defaults.bedrock) &&
                  has(self.spec.defaults.bedrock.guardrailVersion))
              message: >-
                spec.mandatory.bedrock.guardrailVersion and
                spec.defaults.bedrock.guardrailVersion cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.bedrock) &&
                  has(self.spec.mandatory.bedrock.allowedModels) &&
                  has(self.spec.defaults) && has(self.spec.defaults.bedrock) &&
                  has(self.spec.defaults.bedrock.allowedModels))
              message: >-
                spec.mandatory.bedrock.allowedModels and
                spec.defaults.bedrock.allowedModels cannot both be set.
                Set the field in exactly one tier.
            # SageMaker field mutual-exclusivity (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sagemaker) &&
                  has(self.spec.mandatory.sagemaker.instanceType) &&
                  self.spec.mandatory.sagemaker.instanceType != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.sagemaker) &&
                  has(self.spec.defaults.sagemaker.instanceType) &&
                  self.spec.defaults.sagemaker.instanceType != "")
              message: >-
                spec.mandatory.sagemaker.instanceType and
                spec.defaults.sagemaker.instanceType cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sagemaker) &&
                  has(self.spec.mandatory.sagemaker.kmsKeyId) &&
                  self.spec.mandatory.sagemaker.kmsKeyId != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.sagemaker) &&
                  has(self.spec.defaults.sagemaker.kmsKeyId) &&
                  self.spec.defaults.sagemaker.kmsKeyId != "")
              message: >-
                spec.mandatory.sagemaker.kmsKeyId and
                spec.defaults.sagemaker.kmsKeyId cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sagemaker) &&
                  has(self.spec.mandatory.sagemaker.enableNetworkIsolation) &&
                  self.spec.mandatory.sagemaker.enableNetworkIsolation == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.sagemaker) &&
                  has(self.spec.defaults.sagemaker.enableNetworkIsolation) &&
                  self.spec.defaults.sagemaker.enableNetworkIsolation == true)
              message: >-
                spec.mandatory.sagemaker.enableNetworkIsolation and
                spec.defaults.sagemaker.enableNetworkIsolation cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.sagemaker) &&
                  has(self.spec.mandatory.sagemaker.enableInterContainerTrafficEncryption) &&
                  self.spec.mandatory.sagemaker.enableInterContainerTrafficEncryption == true &&
                  has(self.spec.defaults) && has(self.spec.defaults.sagemaker) &&
                  has(self.spec.defaults.sagemaker.enableInterContainerTrafficEncryption) &&
                  self.spec.defaults.sagemaker.enableInterContainerTrafficEncryption == true)
              message: >-
                spec.mandatory.sagemaker.enableInterContainerTrafficEncryption and
                spec.defaults.sagemaker.enableInterContainerTrafficEncryption cannot both be true.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mwaa) &&
                  has(self.spec.mandatory.mwaa.airflowVersion) &&
                  self.spec.mandatory.mwaa.airflowVersion != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.mwaa) &&
                  has(self.spec.defaults.mwaa.airflowVersion) &&
                  self.spec.defaults.mwaa.airflowVersion != "")
              message: >-
                spec.mandatory.mwaa.airflowVersion and spec.defaults.mwaa.airflowVersion
                cannot both be non-empty. Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mwaa) &&
                  has(self.spec.mandatory.mwaa.environmentClass) &&
                  self.spec.mandatory.mwaa.environmentClass != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.mwaa) &&
                  has(self.spec.defaults.mwaa.environmentClass) &&
                  self.spec.defaults.mwaa.environmentClass != "")
              message: >-
                spec.mandatory.mwaa.environmentClass and spec.defaults.mwaa.environmentClass
                cannot both be non-empty. Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mwaa) &&
                  has(self.spec.mandatory.mwaa.webserverAccessMode) &&
                  self.spec.mandatory.mwaa.webserverAccessMode != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.mwaa) &&
                  has(self.spec.defaults.mwaa.webserverAccessMode) &&
                  self.spec.defaults.mwaa.webserverAccessMode != "")
              message: >-
                spec.mandatory.mwaa.webserverAccessMode and spec.defaults.mwaa.webserverAccessMode
                cannot both be non-empty. Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mwaa) &&
                  has(self.spec.mandatory.mwaa.endpointManagement) &&
                  self.spec.mandatory.mwaa.endpointManagement != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.mwaa) &&
                  has(self.spec.defaults.mwaa.endpointManagement) &&
                  self.spec.defaults.mwaa.endpointManagement != "")
              message: >-
                spec.mandatory.mwaa.endpointManagement and spec.defaults.mwaa.endpointManagement
                cannot both be non-empty. Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mwaa) &&
                  has(self.spec.mandatory.mwaa.dagProcessingLogsLevel) &&
                  self.spec.mandatory.mwaa.dagProcessingLogsLevel != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.mwaa) &&
                  has(self.spec.defaults.mwaa.dagProcessingLogsLevel) &&
                  self.spec.defaults.mwaa.dagProcessingLogsLevel != "")
              message: >-
                spec.mandatory.mwaa.dagProcessingLogsLevel and spec.defaults.mwaa.dagProcessingLogsLevel
                cannot both be non-empty. Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mwaa) &&
                  has(self.spec.mandatory.mwaa.schedulerLogsLevel) &&
                  self.spec.mandatory.mwaa.schedulerLogsLevel != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.mwaa) &&
                  has(self.spec.defaults.mwaa.schedulerLogsLevel) &&
                  self.spec.defaults.mwaa.schedulerLogsLevel != "")
              message: >-
                spec.mandatory.mwaa.schedulerLogsLevel and spec.defaults.mwaa.schedulerLogsLevel
                cannot both be non-empty. Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.mwaa) &&
                  has(self.spec.mandatory.mwaa.maxWorkers) &&
                  self.spec.mandatory.mwaa.maxWorkers != 0 &&
                  has(self.spec.defaults) && has(self.spec.defaults.mwaa) &&
                  has(self.spec.defaults.mwaa.maxWorkers) &&
                  self.spec.defaults.mwaa.maxWorkers != 0)
              message: >-
                spec.mandatory.mwaa.maxWorkers and spec.defaults.mwaa.maxWorkers
                cannot both be non-zero. Set the field in exactly one tier.
            # Managed Prometheus field mutual-exclusivity: a field must not appear in both
            # mandatory.managedprometheus and defaults.managedprometheus simultaneously (ADR-015 §3.2).
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.managedprometheus) &&
                  has(self.spec.mandatory.managedprometheus.alias) &&
                  self.spec.mandatory.managedprometheus.alias != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.managedprometheus) &&
                  has(self.spec.defaults.managedprometheus.alias) &&
                  self.spec.defaults.managedprometheus.alias != "")
              message: >-
                spec.mandatory.managedprometheus.alias and
                spec.defaults.managedprometheus.alias cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.managedprometheus) &&
                  has(self.spec.mandatory.managedprometheus.logGroupARN) &&
                  self.spec.mandatory.managedprometheus.logGroupARN != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.managedprometheus) &&
                  has(self.spec.defaults.managedprometheus.logGroupARN) &&
                  self.spec.defaults.managedprometheus.logGroupARN != "")
              message: >-
                spec.mandatory.managedprometheus.logGroupARN and
                spec.defaults.managedprometheus.logGroupARN cannot both be set.
                Set the field in exactly one tier.
            - rule: >-
                !(has(self.spec.mandatory) && has(self.spec.mandatory.quicksight) &&
                  has(self.spec.mandatory.quicksight.importMode) &&
                  self.spec.mandatory.quicksight.importMode != "" &&
                  has(self.spec.defaults) && has(self.spec.defaults.quicksight) &&
                  has(self.spec.defaults.quicksight.importMode) &&
                  self.spec.defaults.quicksight.importMode != "")
              message: >-
                spec.mandatory.quicksight.importMode and spec.defaults.quicksight.importMode
                cannot both be non-empty. Set the field in exactly one tier.
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              default: {}
              properties:
                mandatory:
                  type: object
                  default: {}
                  description: >-
                    Mandatory governance fields. Values here override defaults and instance
                    spec values. Zero-value means no mandatory constraint is enforced.
                  properties:
                    tags:
                      type: object
                      default: {}
                      description: Cloud resource tags applied to all resources.
                      additionalProperties:
                        type: string
                    syncedLabels:
                      type: object
                      default: {}
                      description: >-
                        Labels synced to child resource metadata.labels (kropath.run/ prefix)
                        and to cloud resource tags.
                      additionalProperties:
                        type: string
                    syncedAnnotations:
                      type: object
                      default: {}
                      description: >-
                        Annotations synced to child resource metadata.annotations
                        (kropath.run/ prefix).
                      additionalProperties:
                        type: string
                    namingTemplate:
                      type: string
                      default: ""
                      description: >-
                        Naming template for cloud resource names. Uses {namespace}, {name},
                        {kind} placeholders. Empty string = no org-wide mandatory template.
                    iam:
                      type: object
                      default: {}
                      description: >-
                        IAM-family governance fields applied across all IAM resource types.
                        Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                      properties:
                        permissionsBoundaryArn:
                          type: string
                          description: >-
                            ARN of the IAM permissions boundary policy to attach to all IAM roles
                            and users. Empty string = no mandatory boundary enforced.
                        blockIamUserAccessKeys:
                          type: boolean
                          default: false
                          description: >-
                            When true, blocks creation of IAM user access keys regardless of
                            instance spec. false (zero value) = not enforced.
                        maxSessionDurationSeconds:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 43200
                          description: >-
                            Maximum IAM role session duration in seconds. Clamps instance values
                            above this limit. 0 (zero value) = no mandatory cap enforced.
                    kms:
                      type: object
                      default: {}
                      description: >-
                        KMS-family governance fields applied across all KMS resource types.
                        Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                      properties:
                        enableKeyRotation:
                          type: boolean
                          default: false
                          description: >-
                            When true, org-wide rotation enforcement is enabled. false = not enforced.
                        allowedKeySpecs:
                          type: array
                          description: >-
                            Org-wide allowlist of permitted KMS key specs. Empty list = no restriction.
                          items:
                            type: string
                    s3:
                      type: object
                      default: {}
                      description: >-
                        S3-family governance fields applied across all S3 resource types.
                        Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                      properties:
                        encryptionAlgorithm:
                          type: string
                          description: >-
                            S3 encryption algorithm. Empty string = no mandatory enforcement.
                        kmsKeyArn:
                          type: string
                          description: >-
                            KMS key ARN for S3 encryption. Empty string = no mandatory enforcement.
                        blockPublicAccess:
                          type: boolean
                          default: false
                          description: >-
                            When true, blocks public access regardless of instance spec.
                        versioning:
                          type: string
                          description: >-
                            S3 versioning state. Empty string = no mandatory enforcement.
                        loggingEnabled:
                          type: boolean
                          default: false
                          description: >-
                            When true, enables server access logging regardless of instance spec.
                        logDeliveryBucket:
                          type: string
                          description: >-
                            Target bucket for server access logs. Empty string = no enforcement.
                        enforceHttpsOnly:
                          type: boolean
                          default: false
                          description: >-
                            When true, requires TLS for bucket access regardless of instance spec.
                        objectLockMode:
                          type: string
                          description: >-
                            S3 object lock mode. Empty string = no mandatory enforcement.
                        objectLockRetentionDays:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: >-
                            S3 object lock retention period in days. 0 = no mandatory enforcement.
                    sqs:
                      type: object
                      default: {}
                      description: >-
                        SQS-family governance fields applied across all SQS resource types.
                        Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                      properties:
                        encryptionType:
                          type: string
                          description: >-
                            Org-wide SQS encryption type enforcement. Empty string = no mandatory enforcement.
                            One of: "sqs-managed", "kms", "none".
                        kmsMasterKeyId:
                          type: string
                          description: >-
                            Org-wide mandatory KMS key for all SQS queues. Empty string = no mandatory enforcement.
                        visibilityTimeout:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 43200
                          description: >-
                            Org-wide hard ceiling for visibility timeout in seconds. 0 = no mandatory cap.
                        messageRetentionPeriod:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 1209600
                          description: >-
                            Org-wide hard ceiling for message retention period in seconds. 0 = no mandatory cap.
                        delaySeconds:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 900
                          description: >-
                            Org-wide hard ceiling for message delivery delay in seconds. 0 = no mandatory cap.
                        maximumMessageSize:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 262144
                          description: >-
                            Org-wide hard ceiling for maximum message size in bytes. 0 = no mandatory cap.
                    secretsManager:
                      type: object
                      default: {}
                      description: >-
                        SecretsManager-family governance fields applied across all Secrets Manager
                        resource types. Cascade levels 1-2 in the ten-level priority chain
                        (ADR-015 §5.3). Only kmsKeyID is governed at this level; operational
                        fields (replicaRegions, forceOverwriteReplicaSecret, namingTemplate) are
                        not governed at the KropathConfig level.
                      properties:
                        kmsKeyID:
                          type: string
                          description: >-
                            Org-wide mandatory KMS key ID, ARN, or alias for all Secrets Manager
                            secrets. Empty string = no mandatory encryption key enforced.
                    sns:
                      type: object
                      default: {}
                      description: >-
                        SNS-family governance fields applied at the org-wide mandatory tier.
                        Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                        Only kmsMasterKeyId, signatureVersion, and tracingConfig are governed
                        at the KropathConfig level; topic-specific fields are not.
                      properties:
                        kmsMasterKeyId:
                          type: string
                          description: >-
                            Org-wide mandatory KMS key ID or ARN for SNS topic SSE.
                            Empty string = no mandatory encryption key enforced.
                        signatureVersion:
                          type: string
                          description: >-
                            Org-wide mandatory SNS message signature version ("1" or "2").
                            Empty string = not enforced.
                        tracingConfig:
                          type: string
                          description: >-
                            Org-wide mandatory SNS X-Ray active tracing mode ("Active" or "PassThrough").
                            Empty string = not enforced.
                    dynamodb:
                      type: object
                      default: {}
                      description: >-
                        DynamoDB-family governance fields at the org-wide mandatory tier.
                        Cascade levels 1-2 (ADR-015 §5.3). namingTemplate, syncedLabels,
                        syncedAnnotations are DynamoDBConfig-only fields.
                      properties:
                        encryptionEnabled:
                          type: boolean
                          description: >-
                            Org-wide mandatory SSE enforcement. Omit (nil) = not enforced;
                            false = explicitly disabled. No default — nil detection required.
                        kmsMasterKeyId:
                          type: string
                          description: Org-wide mandatory KMS key ARN/ID. Empty = not enforced.
                        deletionProtectionEnabled:
                          type: boolean
                          description: Org-wide mandatory deletion-protection. Omit (nil) = not enforced.
                        pointInTimeRecoveryEnabled:
                          type: boolean
                          description: Org-wide mandatory PITR enforcement. Omit (nil) = not enforced.
                        billingMode:
                          type: string
                          enum: ["", "PROVISIONED", "PAY_PER_REQUEST"]
                          description: Org-wide mandatory billing mode. Empty = not enforced.
                        tableClass:
                          type: string
                          enum: ["", "STANDARD", "STANDARD_INFREQUENT_ACCESS"]
                          description: Org-wide mandatory table class. Empty = not enforced.
                        contributorInsights:
                          type: string
                          enum: ["", "ENABLE", "DISABLE"]
                          description: Org-wide mandatory Contributor Insights action. Empty = not enforced.
                    eventbridge:
                      type: object
                      default: {}
                      description: >-
                        EventBridge-family governance fields applied across all EventBridge resource
                        types. Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                        namingTemplate, syncedLabels, and syncedAnnotations are EventBridgeConfig-only.
                      properties:
                        archiveRetentionDays:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: >-
                            Org-wide mandatory minimum archive retention in days. 0 = not enforced.
                            When non-zero, no EventBridge archive may use a shorter retention period.
                    cloudwatchlogs:
                      type: object
                      default: {}
                      description: >-
                        CloudWatch Logs-family governance fields applied across all CloudWatch Logs
                        resource types. Cascade levels 1-2 in the ten-level priority chain
                        (ADR-015 §5.3). namingTemplate, syncedLabels, and syncedAnnotations are
                        CloudWatchLogsConfig-only.
                      properties:
                        kmsKeyId:
                          type: string
                          description: >-
                            Org-wide encryption enforcement. Full KMS key ARN (no AWS-managed
                            service key alias for CloudWatch Logs). Empty string = not enforced.
                        retentionDays:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: >-
                            Org-wide mandatory retention period in days. 0 = not enforced;
                            first-non-zero-wins in cascade. Must be one of the CloudWatch Logs
                            allowed retention values when non-zero.
                    cloudtrail:
                      type: object
                      default: {}
                      description: >-
                        CloudTrail-family governance fields applied across all CloudTrail resource
                        types. Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                        namingTemplate, syncedLabels, and syncedAnnotations are
                        CloudTrailConfig-only.
                      properties:
                        isMultiRegionTrail:
                          type: boolean
                          description: >-
                            Org-wide enforcement of multi-region trail setting. false = not enforced;
                            true = all trails must be multi-region. First-true-wins in cascade.
                        enableLogFileValidation:
                          type: boolean
                          description: >-
                            Org-wide enforcement of log file integrity validation. false = not
                            enforced; true = all trails must enable validation. First-true-wins
                            in cascade.
                        retentionPeriod:
                          type: integer
                          format: int64
                          minimum: 0
                          description: >-
                            Org-wide minimum event data store retention in days. 0 = not enforced;
                            first-non-zero-wins in cascade.
                    cloudwatch:
                      type: object
                      default: {}
                      description: >-
                        CloudWatch-family governance fields applied across all CloudWatch
                        resource types. Cascade levels 1-2 in the ten-level priority chain
                        (ADR-015 §5.3). namingTemplate, syncedLabels, and syncedAnnotations are
                        CloudWatchConfig-only.
                      properties:
                        actionsEnabled:
                          type: boolean
                          nullable: true
                          description: >-
                            Org-wide enforcement of CloudWatch alarm actions. null = not enforced;
                            true/false = enforced value applied to all alarm instances.
                        treatMissingData:
                          type: string
                          description: >-
                            Org-wide enforcement of how missing data is treated in alarms.
                            Empty string = not enforced. Valid values: breaching, notBreaching,
                            ignore, missing.
                        outputFormat:
                          type: string
                          description: >-
                            Org-wide enforcement of CloudWatch metric stream output format.
                            Empty string = not enforced. Valid values: json, opentelemetry1.0,
                            opentelemetry0.7.
                        tags:
                          type: object
                          default: {}
                          description: >-
                            Org-wide CloudWatch resource tags. Merged additively with
                            CloudWatchConfig tags; this (stronger) level wins on key conflict.
                          additionalProperties:
                            type: string
                    cloudfront:
                      type: object
                      default: {}
                      description: >-
                        CloudFront-family mandatory governance fields applied across all CloudFront
                        resource types. Cascade levels 1-2 in the ten-level priority chain
                        (ADR-015 §5.3). Only 4 fields are promoted to KropathConfig blanket
                        governance: viewerProtocolPolicy, minimumProtocolVersion, webACLRequired,
                        loggingEnabled. The remaining CloudFront-specific fields (httpVersion,
                        sslSupportMethod, loggingBucket, priceClass, geoRestrictionType,
                        oacSigningBehavior, namingTemplate) live only in CloudFrontConfig profiles.
                      properties:
                        viewerProtocolPolicy:
                          type: string
                          description: >-
                            Org-wide mandatory viewer protocol policy enforcement.
                            Empty string = not enforced; "https-only" | "redirect-to-https".
                        minimumProtocolVersion:
                          type: string
                          description: >-
                            Org-wide mandatory minimum TLS version for HTTPS connections.
                            Empty string = not enforced; e.g. "TLSv1.2_2021".
                        webACLRequired:
                          type: boolean
                          default: false
                          description: >-
                            Enforces WAF association on all CloudFront distributions org-wide
                            when true. false (zero value) = not enforced.
                        loggingEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Enforces access logging on all CloudFront distributions org-wide
                            when true. false (zero value) = not enforced.
                    elb:
                      type: object
                      default: {}
                      description: >-
                        ELB-family mandatory governance fields. Cascade levels 1-2 in the
                        ten-level priority chain (ADR-015 §5.3). Only the three cross-type ELB
                        boolean fields live here; fields specific to ELBConfig
                        (accessLogsS3Bucket, crossZoneEnabled, idleTimeoutSeconds, sslPolicy,
                        namingTemplate, syncedLabels, syncedAnnotations) are not in KropathConfig.
                      properties:
                        deletionProtection:
                          type: boolean
                          default: false
                          description: >-
                            Enforces deletion protection on all ELBs org-wide when true.
                            false (zero value) = not enforced.
                        accessLogsEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Enforces access logging for all ELBs org-wide when true.
                            false (zero value) = not enforced.
                        internalOnly:
                          type: boolean
                          default: false
                          description: >-
                            Forces all ELBs to use internal scheme org-wide when true.
                            false (zero value) = not enforced (both schemes allowed).
                    rds:
                      type: object
                      default: {}
                      description: >-
                        RDS-family mandatory governance fields. Cascade levels 1-2 in the
                        nine-level priority chain (ADR-015 §5.3). Cluster-only fields
                        (serverlessV2ScalingMinCapacity, serverlessV2ScalingMaxCapacity,
                        backtrackWindow) are not in KropathConfig — use RDSConfig for those.
                        namingTemplate, syncedLabels, and syncedAnnotations are RDSConfig-only.
                      properties:
                        storageEncrypted:
                          type: boolean
                          description: >-
                            Force encryption at rest on all RDS instances/clusters org-wide.
                            Omit (nil) = not enforced; false = explicitly disabled.
                            No default — nil (*bool) detection required.
                        kmsKeyID:
                          type: string
                          description: >-
                            Org-wide mandatory KMS key ARN for RDS encryption. Empty = not enforced.
                        deletionProtection:
                          type: boolean
                          description: >-
                            Force deletion protection on all RDS instances/clusters org-wide.
                            Omit (nil) = not enforced.
                        backupRetentionPeriod:
                          type: integer
                          format: int64
                          default: 0
                          description: >-
                            Org-wide mandatory backup retention floor in days. 0 = not enforced.
                            Minimum-floor semantics: max(mandatory, instance) applied in RGD CEL.
                        multiAZ:
                          type: boolean
                          description: >-
                            Force Multi-AZ deployment on all RDS instances org-wide.
                            Omit (nil) = not enforced.
                        publiclyAccessible:
                          type: boolean
                          description: >-
                            Control public endpoint exposure org-wide. Omit (nil) = not enforced;
                            false (explicit pointer) = forced private-only across all instances.
                        storageType:
                          type: string
                          description: >-
                            Org-wide mandatory storage type (e.g. "gp3", "io1"). Empty = not enforced.
                        autoMinorVersionUpgrade:
                          type: boolean
                          description: >-
                            Force auto minor version upgrades org-wide. Omit (nil) = not enforced.
                        copyTagsToSnapshot:
                          type: boolean
                          description: >-
                            Force tag copying to snapshots org-wide. Omit (nil) = not enforced.
                        performanceInsightsEnabled:
                          type: boolean
                          description: >-
                            Force Performance Insights on org-wide. Omit (nil) = not enforced.
                        enableIAMDatabaseAuthentication:
                          type: boolean
                          description: >-
                            Force IAM database authentication org-wide. Omit (nil) = not enforced.
                        manageMasterUserPassword:
                          type: boolean
                          description: >-
                            Force Secrets Manager password management org-wide. Omit (nil) = not enforced.
                        namingTemplate:
                          type: string
                          description: >-
                            Org-wide RDS naming template (e.g. "{namespace}-{name}"). Empty = not enforced.
                        tags:
                          type: object
                          default: {}
                          description: Mandatory cloud resource tags applied to all RDS resources.
                          additionalProperties:
                            type: string
                    autoscaling:
                      type: object
                      default: {}
                      description: >-
                        Auto Scaling-family mandatory governance fields. Cascade levels 1-2 in the
                        nine-level priority chain (ADR-015 §5.3). Only the two cross-type boolean
                        fields are governed here; all other Auto Scaling fields (healthCheckType,
                        healthCheckGracePeriod, maxInstanceLifetime, namingTemplate,
                        syncedLabels, syncedAnnotations) are AutoScalingConfig-only.
                      properties:
                        newInstancesProtectedFromScaleIn:
                          type: boolean
                          default: false
                          description: >-
                            Force instance scale-in protection on all Auto Scaling groups org-wide.
                            false (zero value) = not enforced.
                        capacityRebalance:
                          type: boolean
                          default: false
                          description: >-
                            Force Spot capacity rebalancing on all Auto Scaling groups org-wide.
                            false (zero value) = not enforced.
                    lambda:
                      type: object
                      default: {}
                      description: >-
                        Lambda-family mandatory governance fields applied across all Lambda
                        resource types. Cascade levels 1-2 in the ten-level priority chain
                        (ADR-015 §5.3). namingTemplate, syncedLabels, and syncedAnnotations are
                        LambdaConfig-only.
                      properties:
                        runtime:
                          type: string
                          description: >-
                            Org-wide mandatory Lambda runtime enforcement (e.g. "python3.12").
                            Empty string = not enforced; instances cannot override when set.
                        memorySize:
                          type: integer
                          format: int64
                          default: 0
                          description: >-
                            Org-wide hard memory ceiling in MB. 0 = not enforced;
                            positive value = maximum memorySize for all functions in scope.
                        timeout:
                          type: integer
                          format: int64
                          default: 0
                          description: >-
                            Org-wide hard execution timeout ceiling in seconds. 0 = not enforced;
                            positive value = maximum timeout for all functions in scope.
                        tracingMode:
                          type: string
                          description: >-
                            Org-wide mandatory AWS X-Ray tracing mode enforcement.
                            Empty string = not enforced; "PassThrough" | "Active".
                        kmsKeyArn:
                          type: string
                          description: >-
                            Org-wide mandatory KMS key ARN for Lambda environment variable encryption.
                            Empty string = not enforced; instances cannot use a different key when set.
                    ec2:
                      type: object
                      default: {}
                      description: >-
                        EC2-family mandatory governance fields. Cascade levels 1-2 in the
                        ten-level priority chain (ADR-015 §5.3). Only the three cross-type EC2
                        boolean fields live here; fields specific to EC2Config
                        (flowLogTrafficType, flowLogMaxAggregationInterval, restrictPublicIpOnLaunch,
                        ebsDefaultKmsKeyId, publicIpRestricted, allowSourceDestCheckDisable,
                        namingTemplate, syncedLabels, syncedAnnotations) are not in KropathConfig.
                      properties:
                        flowLogsRequired:
                          type: boolean
                          default: false
                          description: >-
                            When true, requires VPC Flow Logs on all EC2 resources org-wide.
                            false (zero value) = not enforced.
                        imdsv2Required:
                          type: boolean
                          default: false
                          description: >-
                            When true, enforces IMDSv2-only (token-required) on all EC2 instances
                            org-wide. false (zero value) = not enforced.
                        ebsEncryptionRequired:
                          type: boolean
                          default: false
                          description: >-
                            When true, enforces EBS volume encryption on all EC2 instances org-wide.
                            false (zero value) = not enforced.
                    ecs:
                      type: object
                      default: {}
                      description: >-
                        ECS-family mandatory governance fields. Cascade levels 1-2 in the
                        ten-level priority chain (ADR-015 §5.3). Only the two cross-type fields
                        are governed here; all other ECS fields (defaultPlatformVersion,
                        defaultNetworkMode, defaultTaskCPU, defaultTaskMemory, namingTemplate,
                        syncedLabels, syncedAnnotations) are ECSConfig-only.
                      properties:
                        containerInsights:
                          type: boolean
                          default: false
                          description: >-
                            Force Container Insights enabled on all ECS clusters org-wide.
                            false (zero value) = not enforced.
                        defaultLaunchType:
                          type: string
                          description: >-
                            Org-wide mandatory ECS launch type enforcement (EC2 | FARGATE).
                            Empty string = not enforced; instances cannot override when set.
                    apigatewayv2:
                      type: object
                      default: {}
                      description: >-
                        API Gateway V2-family mandatory governance fields. Cascade levels 1-2
                        in the ten-level priority chain (ADR-015 §5.3). Only the three
                        cross-type fields are governed here; all other ApiGatewayV2 fields
                        are ApiGatewayV2Config-only.
                      properties:
                        corsEnabled:
                          type: boolean
                          default: false
                          description: >-
                            When true, all HTTP APIs must define a corsConfiguration org-wide.
                            false (zero value) = not enforced.
                        disableExecuteApiEndpoint:
                          type: boolean
                          default: false
                          description: >-
                            When true, all APIs must disable the default execute-api endpoint org-wide.
                            false (zero value) = not enforced.
                        minimumTlsVersion:
                          type: string
                          description: >-
                            Org-wide mandatory minimum TLS version for all custom domain names.
                            Empty string = not enforced. Valid values: TLS_1_0, TLS_1_2.
                    elasticache:
                      type: object
                      default: {}
                      description: >-
                        ElastiCache-family mandatory governance fields applied across all ElastiCache
                        resource types. Cascade levels 1-2 in the ten-level priority chain
                        (ADR-015 §5.3). Only 2 fields are promoted to KropathConfig blanket
                        governance: atRestEncryptionEnabled, transitEncryptionEnabled.
                        All other ElastiCache-specific fields live only in ElastiCacheConfig profiles.
                      properties:
                        atRestEncryptionEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Forces encryption at rest on all ElastiCache caches org-wide when true.
                            false (zero value) = not enforced. IMMUTABLE on ReplicationGroup after creation.
                        transitEncryptionEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Forces TLS in-transit on all ElastiCache caches org-wide when true.
                            false (zero value) = not enforced.
                    ecr:
                      type: object
                      default: {}
                      description: >-
                        ECR-family mandatory governance fields. Cascade levels 1-2 in the
                        ten-level priority chain (ADR-015 §5.3). Only the three cross-type fields
                        are governed here; all other ECR fields (kmsKeyID, namingTemplate,
                        syncedLabels, syncedAnnotations) are ECRConfig-only.
                      properties:
                        imageTagMutability:
                          type: string
                          description: >-
                            Org-wide mandatory image tag mutability enforcement (IMMUTABLE | MUTABLE).
                            Empty string = not enforced; instances cannot override when set.
                        encryptionType:
                          type: string
                          description: >-
                            Org-wide mandatory repository encryption type (AES256 | KMS).
                            Empty string = not enforced; instances cannot override when set.
                        lifecyclePolicy:
                          type: string
                          description: >-
                            Org-wide mandatory lifecycle policy JSON template.
                            Empty string = not enforced; instances cannot override when set.
                    stepfunctions:
                      type: object
                      default: {}
                      description: >-
                        Mandatory Step Functions-family governance fields. Cascade levels 1-2
                        (ADR-015 §5.3). Only loggingLevel is governed at KropathConfig level;
                        tracingEnabled and includeExecutionData are StepFunctionsConfig-only.
                      properties:
                        loggingLevel:
                          type: string
                          description: >-
                            Org-wide mandatory execution logging level.
                            "" = not enforced; instances cannot override when set.
                            Valid values: "" | "ALL" | "ERROR" | "FATAL" | "OFF".
                        tags:
                          type: object
                          default: {}
                          description: Org-wide mandatory Step Functions cloud resource tags.
                          additionalProperties:
                            type: string
                    managedprometheus:
                      type: object
                      default: {}
                      description: >-
                        Managed Prometheus-family mandatory governance fields applied across all
                        Managed Prometheus resource types. Cascade levels 1-2 in the ten-level
                        priority chain (ADR-015 §5.3). Only alias and logGroupARN are promoted
                        to KropathConfig blanket governance; namingTemplate, syncedLabels, and
                        syncedAnnotations are ManagedPrometheusConfig-only.
                      properties:
                        alias:
                          type: string
                          description: >-
                            Org-wide mandatory workspace alias enforcement. Empty string = not
                            enforced; instances cannot override when set.
                        logGroupARN:
                          type: string
                          description: >-
                            Org-wide mandatory CloudWatch log group ARN for all AMP workspaces.
                            Empty string = not enforced. Must be a full CloudWatch Logs ARN.
                    memorydb:
                      type: object
                      default: {}
                      description: >-
                        MemoryDB-family mandatory governance fields applied across all MemoryDB
                        resource types. Cascade levels 1-2 in the ten-level priority chain
                        (ADR-015 §5.3). 3 fields are promoted to KropathConfig blanket governance:
                        tlsEnabled, allowedNodeTypes, snapshotRetentionLimit.
                        All other MemoryDB-specific fields live only in MemoryDBConfig profiles.
                      properties:
                        tlsEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Forces TLS in-transit on all MemoryDB clusters org-wide when true.
                            false (zero value) = not enforced. IMMUTABLE on MemoryDB Cluster after creation.
                        allowedNodeTypes:
                          type: array
                          description: >-
                            Org-wide node type restriction. [] = no restriction;
                            non-empty = explicit allowlist applied to all MemoryDB clusters.
                          items:
                            type: string
                        snapshotRetentionLimit:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: >-
                            Org-wide minimum snapshot retention floor in days.
                            0 = not enforced; >0 enforces minimum retention across all clusters.
                    mq:
                      type: object
                      default: {}
                      description: >-
                        MQ-family mandatory governance fields applied across all Amazon MQ
                        resource types. Cascade levels 1-2 in the ten-level priority chain
                        (ADR-015 §5.3). 9 fields are promoted to KropathConfig blanket governance:
                        engineType, authenticationStrategy, deploymentMode, publiclyAccessible,
                        autoMinorVersionUpgrade, logsGeneral, logsAudit, encryptionUseAWSOwnedKey,
                        storageType.
                      properties:
                        engineType:
                          type: string
                          description: >-
                            Org-wide mandatory broker engine type. "" = not enforced.
                            Valid: "" | "ACTIVEMQ" | "RABBITMQ".
                        authenticationStrategy:
                          type: string
                          description: >-
                            Org-wide mandatory authentication strategy. "" = not enforced.
                            Valid: "" | "simple" | "ldap".
                        deploymentMode:
                          type: string
                          description: >-
                            Org-wide mandatory broker deployment mode. "" = not enforced.
                            Valid: "" | "SINGLE_INSTANCE" | "ACTIVE_STANDBY_MULTI_AZ" |
                            "CLUSTER_MULTI_AZ".
                        publiclyAccessible:
                          type: boolean
                          description: >-
                            Forces public accessibility on all MQ brokers org-wide when true.
                            false (zero value) = not enforced.
                        autoMinorVersionUpgrade:
                          type: boolean
                          description: >-
                            Mandates automatic minor version upgrades org-wide when true.
                            false (zero value) = not enforced.
                        logsGeneral:
                          type: boolean
                          description: >-
                            Mandates general log publishing to CloudWatch Logs org-wide when true.
                            false (zero value) = not enforced.
                        logsAudit:
                          type: boolean
                          description: >-
                            Mandates audit log publishing to CloudWatch Logs org-wide when true.
                            false (zero value) = not enforced.
                        encryptionUseAWSOwnedKey:
                          type: boolean
                          description: >-
                            Mandates use of AWS-owned KMS key for encryption org-wide when true.
                            false (zero value) = not enforced.
                        storageType:
                          type: string
                          description: >-
                            Org-wide mandatory storage type for ActiveMQ brokers. "" = not enforced.
                            Valid: "" | "efs" | "io1".
                    msk:
                      type: object
                      default: {}
                      description: >-
                        Mandatory MSK-family governance fields. Cascade levels 1-2
                        (ADR-015 §5.3). Five fields are governed at KropathConfig level;
                        instanceType and namingTemplate are MSKConfig-only.
                      properties:
                        kafkaVersion:
                          type: string
                          description: >-
                            Org-wide mandatory Kafka version (e.g. "3.7.0").
                            "" = not enforced; instances cannot override when set.
                        enhancedMonitoring:
                          type: string
                          description: >-
                            Org-wide mandatory CloudWatch monitoring level.
                            "" = not enforced. Valid: "" | "DEFAULT" | "PER_BROKER" |
                            "PER_TOPIC_PER_BROKER" | "PER_TOPIC_PER_PARTITION".
                        encryptionInTransitClientBroker:
                          type: string
                          description: >-
                            Org-wide mandatory client-broker encryption mode.
                            "" = not enforced. Valid: "" | "TLS" | "TLS_PLAINTEXT" | "PLAINTEXT".
                        encryptionInTransitInCluster:
                          type: string
                          description: >-
                            Org-wide mandatory inter-broker encryption (three-state string).
                            "" = not enforced (semantically distinct from "false").
                            Valid: "" | "true" | "false".
                        encryptionAtRestKmsKeyId:
                          type: string
                          description: >-
                            Org-wide mandatory KMS key ARN for EBS encryption at rest.
                            "" = not enforced.
                        tags:
                          type: object
                          default: {}
                          description: Org-wide mandatory MSK cloud resource tags.
                          additionalProperties:
                            type: string
                    certificateManager:
                      type: object
                      default: {}
                      description: >-
                        Certificate Manager family governance fields applied at the org-wide
                        mandatory tier. Cascade levels 1-2 in the ten-level priority chain
                        (ADR-015 §5.3). Only keyAlgorithm and certificateTransparencyLogging
                        are governed at the KropathConfig level; usageMode and
                        keyStorageSecurityStandard are per-CA choices and belong in ACMConfig
                        or the instance spec (family design §8).
                      properties:
                        keyAlgorithm:
                          type: string
                          description: >-
                            Org-wide mandatory key algorithm for all ACM certificates and private
                            CAs. Empty string = not enforced. Valid: RSA_2048 | EC_prime256v1 |
                            EC_secp384r1.
                        certificateTransparencyLogging:
                          type: string
                          description: >-
                            Org-wide mandatory CT logging preference. Empty string = not enforced.
                            Valid: ENABLED | DISABLED. Only meaningful for public certificates.
                    documentdb:
                      type: object
                      default: {}
                      description: >-
                        Mandatory DocumentDB-family governance fields. Cascade levels 1-2
                        (ADR-015 §5.3). Four fields are governed at KropathConfig level;
                        remaining DocumentDBConfig fields are per-type choices.
                      properties:
                        storageEncrypted:
                          type: boolean
                          default: false
                          description: >-
                            Org-wide mandatory encryption-at-rest enforcement for DocumentDB clusters.
                            false = not enforced; true = all DocumentDB clusters must use storage encryption.
                        deletionProtection:
                          type: boolean
                          default: false
                          description: >-
                            Org-wide mandatory deletion-protection enforcement for DocumentDB clusters.
                            false = not enforced; true = all DocumentDB clusters must have deletion protection enabled.
                        allowedInstanceClasses:
                          type: array
                          description: >-
                            Org-wide mandatory DocumentDB instance class allowlist. [] = no restriction.
                            When non-empty, controller rejects instances whose dbInstanceClass is not in this list.
                          items:
                            type: string
                        backupRetentionPeriod:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: >-
                            Org-wide mandatory minimum backup retention in days.
                            0 = not enforced; >0 enforces minimum retention across all DocumentDB clusters.
                    opensearch:
                      type: object
                      default: {}
                      description: >-
                        Mandatory OpenSearch-family governance fields. Cascade levels 1-2
                        (ADR-015 §5.3). Four fields are governed at KropathConfig level;
                        remaining OpenSearchConfig fields are per-profile choices.
                      properties:
                        encryptionAtRestEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Org-wide mandatory encryption-at-rest enforcement for OpenSearch domains.
                            false = not enforced; true = all OpenSearch domains must use storage encryption.
                            IMMUTABLE on domain after creation.
                        nodeToNodeEncryptionEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Org-wide mandatory node-to-node encryption enforcement for OpenSearch domains.
                            false = not enforced; true = all domains must encrypt inter-node traffic.
                            IMMUTABLE on domain after creation.
                        enforceHTTPS:
                          type: boolean
                          default: false
                          description: >-
                            Org-wide mandatory HTTPS-only access enforcement for OpenSearch domains.
                            false = not enforced; true = all domains must require HTTPS.
                        tlsSecurityPolicy:
                          type: string
                          description: >-
                            Org-wide mandatory minimum TLS security policy for OpenSearch domains.
                            Empty string = not enforced.
                            Valid: Policy-Min-TLS-1-0-2019-10 | Policy-Min-TLS-1-2-2019-07 |
                            Policy-Min-TLS-1-2-PFS-2023-10.
                    dsql:
                      type: object
                      default: {}
                      description: >-
                        Mandatory Aurora DSQL governance fields. Cascade levels 1-2
                        (ADR-015 §5.3). deletionProtectionEnabled is the only field governed
                        at KropathConfig level for DSQL.
                      properties:
                        deletionProtectionEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Org-wide mandatory deletion-protection enforcement for Aurora DSQL clusters.
                            false = not enforced; true = all DSQL clusters must have deletion protection enabled.
                    route53:
                      type: object
                      default: {}
                      description: >-
                        Mandatory Route53 governance fields. Cascade levels 1-2
                        (ADR-015 §5.3). These four fields are the only Route53 governance
                        enforced at the KropathConfig level.
                      properties:
                        defaultTTL:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: >-
                            Org-wide mandatory default TTL in seconds for DNS records.
                            0 = not enforced. Does not apply to alias records.
                        healthCheckRequestInterval:
                          type: integer
                          format: int64
                          default: 0
                          description: >-
                            Org-wide mandatory health check polling interval in seconds.
                            0 = not enforced. Valid non-zero values: 10 (fast) or 30 (standard).
                        healthCheckFailureThreshold:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 10
                          description: >-
                            Org-wide mandatory consecutive-failure count before marking a health
                            check unhealthy. 0 = not enforced. Valid non-zero range: 1–10.
                        resolverEndpointType:
                          type: string
                          description: >-
                            Org-wide mandatory IP address family constraint for Resolver endpoints.
                            Empty = not enforced. Valid values: IPV4, IPV6, DUALSTACK.
                    ssm:
                      type: object
                      default: {}
                      description: >-
                        SSM-family governance fields applied at the org-wide mandatory tier.
                        Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                        Only parameterType, parameterTier, keyID, and allowedDocumentTypes are
                        governed at the KropathConfig level; documentType, operatingSystem,
                        patch-specific fields, and namingTemplate belong in SSMConfig profiles.
                      properties:
                        parameterType:
                          type: string
                          description: >-
                            Org-wide mandatory parameter type enforcement (String | StringList |
                            SecureString). Empty string = not enforced; instances cannot override
                            when set. Note: type is immutable after parameter creation.
                        parameterTier:
                          type: string
                          description: >-
                            Org-wide mandatory parameter tier enforcement (Standard | Advanced |
                            Intelligent-Tiering). Empty string = not enforced. Advancing to Advanced
                            is irreversible without deletion and recreation.
                        keyID:
                          type: string
                          description: >-
                            Org-wide mandatory KMS key ARN, ID, or alias for SecureString
                            encryption. Empty string = not enforced.
                        allowedDocumentTypes:
                          type: array
                          description: >-
                            Org-wide restriction on allowed SSM document types. [] = no restriction;
                            non-empty = allowlist applied to all SSM documents org-wide.
                          items:
                            type: string
                    cognito:
                      type: object
                      default: {}
                      description: >-
                        Cognito-family governance fields applied at the org-wide mandatory tier.
                        Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                        Only mfaConfiguration, deletionProtection, advancedSecurityMode, and
                        passwordPolicy are governed at the KropathConfig level; namingTemplate,
                        syncedLabels, and syncedAnnotations belong in CognitoConfig profiles.
                      properties:
                        mfaConfiguration:
                          type: string
                          description: >-
                            Org-wide mandatory MFA level enforcement ("OFF", "OPTIONAL", "ON").
                            Empty string = not enforced; instances cannot override when set.
                        deletionProtection:
                          type: string
                          description: >-
                            Org-wide mandatory deletion protection state ("ACTIVE", "INACTIVE").
                            Empty string = not enforced.
                        advancedSecurityMode:
                          type: string
                          description: >-
                            Org-wide mandatory advanced security mode ("OFF", "AUDIT", "ENFORCED").
                            Empty string = not enforced.
                        passwordPolicy:
                          type: object
                          default: {}
                          description: Cognito password policy governance sub-fields.
                          properties:
                            minimumLength:
                              type: integer
                              format: int64
                              default: 0
                              description: Minimum password length. 0 = not enforced; valid range 6-99.
                            requireLowercase:
                              type: boolean
                              description: Require at least one lowercase letter. Omit = not enforced.
                            requireNumbers:
                              type: boolean
                              description: Require at least one digit. Omit = not enforced.
                            requireSymbols:
                              type: boolean
                              description: Require at least one symbol. Omit = not enforced.
                            requireUppercase:
                              type: boolean
                              description: Require at least one uppercase letter. Omit = not enforced.
                            temporaryPasswordValidityDays:
                              type: integer
                              format: int64
                              default: 0
                              description: Temporary password expiry in days. 0 = not enforced; valid range 1-365.
                    keyspaces:
                      type: object
                      default: {}
                      description: >-
                        Keyspaces-family governance fields applied at the org-wide mandatory tier.
                        Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                        Only replicationStrategy, throughputMode, encryptionType,
                        pointInTimeRecovery, and ttlEnabled are governed at the KropathConfig level;
                        namingTemplate, syncedLabels, and syncedAnnotations belong in
                        KeyspacesConfig profiles.
                      properties:
                        replicationStrategy:
                          type: string
                          description: >-
                            Org-wide mandatory replication strategy enforcement
                            ("SINGLE_REGION" or "MULTI_REGION").
                            Empty string = not enforced; instances cannot override when set.
                        throughputMode:
                          type: string
                          description: >-
                            Org-wide mandatory throughput mode enforcement
                            ("PAY_PER_REQUEST" or "PROVISIONED").
                            Empty string = not enforced.
                        encryptionType:
                          type: string
                          description: >-
                            Org-wide mandatory encryption type enforcement
                            ("AWS_OWNED_KMS_KEY" or "CUSTOMER_MANAGED_KMS_KEY").
                            Empty string = not enforced.
                        pointInTimeRecovery:
                          type: boolean
                          default: false
                          description: >-
                            Enforce point-in-time recovery on all Keyspaces tables org-wide.
                            false = not enforced.
                        ttlEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Enforce TTL enabled on all Keyspaces tables org-wide.
                            false = not enforced.
                    bedrock:
                      type: object
                      default: {}
                      description: >-
                        Bedrock-family governance fields applied at the org-wide mandatory tier.
                        Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                        Only guardrailIdentifier, guardrailVersion, and allowedModels are governed
                        at the KropathConfig level; foundationModel, maxIterations, maxTokens,
                        timeoutSeconds, and idleSessionTTLInSeconds belong in BedrockConfig profiles.
                      properties:
                        guardrailIdentifier:
                          type: string
                          description: >-
                            Org-wide mandatory Bedrock guardrail identifier. Empty string = not enforced;
                            non-empty forces all Bedrock agents and harnesses to use this guardrail.
                            Must be paired with guardrailVersion.
                        guardrailVersion:
                          type: string
                          description: >-
                            Org-wide mandatory Bedrock guardrail version. Empty string = not enforced.
                            Paired with guardrailIdentifier; must be set together.
                        allowedModels:
                          type: array
                          description: >-
                            Org-wide model restriction. [] = no restriction; non-empty = allowlist
                            of AWS Bedrock model IDs applied across all Bedrock resources.
                          items:
                            type: string
                        tags:
                          type: object
                          default: {}
                          additionalProperties:
                            type: string
                          description: Cloud resource tags merged into all Bedrock resources org-wide (mandatory tier).
                    sagemaker:
                      type: object
                      description: >-
                        Org-wide mandatory SageMaker governance fields. Highest-priority source in the
                        ADR-010 cascade for SageMaker resources. Zero-value = not enforced.
                      properties:
                        instanceType:
                          type: string
                          description: Forces this SageMaker instance type org-wide. Empty = not enforced.
                        kmsKeyId:
                          type: string
                          description: Forces this KMS key for SageMaker storage encryption org-wide. Empty = not enforced.
                        enableNetworkIsolation:
                          type: boolean
                          description: Forces network isolation on all SageMaker resources org-wide. false = not enforced.
                        enableInterContainerTrafficEncryption:
                          type: boolean
                          description: Forces inter-container traffic encryption org-wide. false = not enforced.
                    pipes:
                      type: object
                      default: {}
                      description: >-
                        Org-wide mandatory EventBridge Pipes governance fields. Highest-priority source
                        in the ADR-010 cascade for Pipes resources. Zero-value = not enforced.
                      properties:
                        desiredState:
                          type: string
                          default: ""
                          description: Forces this desired state on all EventBridge Pipes org-wide (RUNNING | STOPPED). Empty = not enforced.
                    codeartifact:
                      type: object
                      default: {}
                      description: >-
                        Org-wide mandatory CodeArtifact governance fields. Highest-priority source
                        in the ADR-010 cascade for CodeArtifact resources. Zero-value = not enforced.
                      properties:
                        encryptionKey:
                          type: string
                          default: ""
                          description: Forces this KMS key ARN for CodeArtifact domain encryption org-wide. Empty = not enforced.
                    mwaa:
                      type: object
                      default: {}
                      description: >-
                        Org-wide mandatory MWAA governance fields. Highest-priority source in the
                        ADR-010 cascade for MWAA resources. Zero-value = not enforced.
                      properties:
                        airflowVersion:
                          type: string
                          default: ""
                          description: Forces this Airflow version org-wide. Empty = not enforced.
                        environmentClass:
                          type: string
                          default: ""
                          description: Forces this environment class org-wide. Empty = not enforced.
                        webserverAccessMode:
                          type: string
                          default: ""
                          description: Forces this web server access mode org-wide (PRIVATE_ONLY | PUBLIC_ONLY). Empty = not enforced.
                        endpointManagement:
                          type: string
                          default: ""
                          description: Forces this VPC endpoint management model org-wide (SERVICE | CUSTOMER). Empty = not enforced.
                        kmsKeyARN:
                          type: string
                          default: ""
                          description: Forces this KMS key ARN for MWAA encryption org-wide. Empty = not enforced.
                        dagProcessingLogsEnabled:
                          type: boolean
                          description: Forces DAG processing logging state org-wide. Absent (nil) = not enforced.
                        dagProcessingLogsLevel:
                          type: string
                          default: ""
                          description: Forces DAG processing log level org-wide. Empty = not enforced.
                        schedulerLogsEnabled:
                          type: boolean
                          description: Forces scheduler logging state org-wide. Absent (nil) = not enforced.
                        schedulerLogsLevel:
                          type: string
                          default: ""
                          description: Forces scheduler log level org-wide. Empty = not enforced.
                        taskLogsEnabled:
                          type: boolean
                          description: Forces task logging state org-wide. Absent (nil) = not enforced.
                        taskLogsLevel:
                          type: string
                          default: ""
                          description: Forces task log level org-wide. Empty = not enforced.
                        webserverLogsEnabled:
                          type: boolean
                          description: Forces web server logging state org-wide. Absent (nil) = not enforced.
                        webserverLogsLevel:
                          type: string
                          default: ""
                          description: Forces web server log level org-wide. Empty = not enforced.
                        workerLogsEnabled:
                          type: boolean
                          description: Forces worker logging state org-wide. Absent (nil) = not enforced.
                        workerLogsLevel:
                          type: string
                          default: ""
                          description: Forces worker log level org-wide. Empty = not enforced.
                        maxWorkers:
                          type: integer
                          format: int64
                          default: 0
                          description: Forces this maximum worker count org-wide. 0 = not enforced.
                        minWorkers:
                          type: integer
                          format: int64
                          default: 0
                          description: Forces this minimum worker count org-wide. 0 = not enforced.
                        maxWebservers:
                          type: integer
                          format: int64
                          default: 0
                          description: Forces this maximum web server count org-wide. 0 = not enforced.
                        minWebservers:
                          type: integer
                          format: int64
                          default: 0
                          description: Forces this minimum web server count org-wide. 0 = not enforced.
                        schedulers:
                          type: integer
                          format: int64
                          default: 0
                          description: Forces this scheduler count org-wide. 0 = not enforced.
                        weeklyMaintenanceWindowStart:
                          type: string
                          default: ""
                          description: Forces this maintenance window start org-wide. Empty = not enforced.
                        namingTemplate:
                          type: string
                          default: ""
                          description: Forces this naming template for MWAA environments org-wide. Empty = not enforced.
                    ram:
                      type: object
                      default: {}
                      description: >-
                        RAM-family governance fields applied at the org-wide mandatory tier.
                        Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                        Only allowExternalPrincipals and allowedResourceTypes are governed at
                        the KropathConfig level; namingTemplate, tags, syncedLabels, and
                        syncedAnnotations belong in RAMConfig profiles.
                      properties:
                        allowExternalPrincipals:
                          type: boolean
                          nullable: true
                          description: >-
                            Org-wide mandatory external-principals enforcement. nil = not enforced;
                            false = mandate disallow for all RAM shares; true = mandate allow.
                        allowedResourceTypes:
                          type: array
                          description: >-
                            Org-wide allowlist of RAM-shareable resource types in
                            <service>:<resourceType> format (e.g. ec2:Subnet). Empty = no
                            org-wide restriction enforced at this tier.
                          items:
                            type: string
                    quicksight:
                      type: object
                      default: {}
                      description: >-
                        QuickSight-family governance fields for mandatory enforcement.
                        Cascade levels 1-2 in the ten-level priority chain (ADR-015 §5.3).
                      properties:
                        importMode:
                          type: string
                          description: >-
                            Enforced dataset import mode org-wide. Empty = not enforced.
                            Allowed values: SPICE, DIRECT_QUERY.
                            Cannot be set together with defaults.quicksight.importMode.

                defaults:
                  type: object
                  default: {}
                  description: >-
                    Default governance fields. Applied when no higher-priority source sets the field.
                    Zero-value means no default is provided at this level.
                  properties:
                    tags:
                      type: object
                      default: {}
                      description: Default cloud resource tags.
                      additionalProperties:
                        type: string
                    syncedLabels:
                      type: object
                      default: {}
                      description: Default labels synced to child resources and cloud tags.
                      additionalProperties:
                        type: string
                    syncedAnnotations:
                      type: object
                      default: {}
                      description: Default annotations synced to child resources.
                      additionalProperties:
                        type: string
                    namingTemplate:
                      type: string
                      default: ""
                      description: Default naming template when no mandatory template is set.
                    iam:
                      type: object
                      default: {}
                      description: >-
                        Default IAM-family governance fields. Cascade levels 8-9 in the
                        ten-level priority chain (ADR-015 §5.3). Applied only when no
                        higher-priority source sets the field.
                      properties:
                        permissionsBoundaryArn:
                          type: string
                          description: >-
                            Default permissions boundary ARN. Used when no mandatory source
                            sets this field. Empty string = no default boundary.
                        blockIamUserAccessKeys:
                          type: boolean
                          default: false
                          description: >-
                            Default block for IAM user access key creation. Used when no
                            mandatory source sets this field.
                        maxSessionDurationSeconds:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 43200
                          description: >-
                            Default maximum IAM role session duration in seconds. 0 = no default.
                    kms:
                      type: object
                      default: {}
                      description: >-
                        Default KMS-family governance fields. Cascade levels 8-9 in the
                        ten-level priority chain (ADR-015 §5.3). Applied only when no
                        higher-priority source sets the field.
                      properties:
                        enableKeyRotation:
                          type: boolean
                          default: false
                          description: Default KMS rotation enforcement toggle.
                        allowedKeySpecs:
                          type: array
                          description: Default KMS key spec allowlist.
                          items:
                            type: string
                    s3:
                      type: object
                      default: {}
                      description: >-
                        Default S3-family governance fields. Cascade levels 8-9 in the
                        ten-level priority chain (ADR-015 §5.3). Applied only when no
                        higher-priority source sets the field.
                      properties:
                        encryptionAlgorithm:
                          type: string
                          description: Default S3 encryption algorithm.
                        kmsKeyArn:
                          type: string
                          description: Default KMS key ARN for S3 encryption.
                        blockPublicAccess:
                          type: boolean
                          default: false
                          description: Default block-public-access setting.
                        versioning:
                          type: string
                          description: Default S3 versioning state.
                        loggingEnabled:
                          type: boolean
                          default: false
                          description: Default server access logging setting.
                        logDeliveryBucket:
                          type: string
                          description: Default server access log target bucket.
                        enforceHttpsOnly:
                          type: boolean
                          default: false
                          description: Default HTTPS-only enforcement setting.
                        objectLockMode:
                          type: string
                          description: Default object lock mode.
                        objectLockRetentionDays:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: Default object lock retention days.
                    sqs:
                      type: object
                      default: {}
                      description: >-
                        Default SQS-family governance fields. Cascade levels 8-9 in the
                        ten-level priority chain (ADR-015 §5.3). Applied only when no
                        higher-priority source sets the field.
                      properties:
                        encryptionType:
                          type: string
                          description: >-
                            Default SQS encryption type. Empty string = no org-wide default.
                        kmsMasterKeyId:
                          type: string
                          description: >-
                            Default KMS key for SQS encryption. Empty string = no org-wide default.
                        visibilityTimeout:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 43200
                          description: >-
                            Default visibility timeout in seconds. 0 = no org-wide default.
                        messageRetentionPeriod:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 1209600
                          description: >-
                            Default message retention period in seconds. 0 = no org-wide default.
                        delaySeconds:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 900
                          description: >-
                            Default message delivery delay in seconds. 0 = no org-wide default.
                        maximumMessageSize:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 262144
                          description: >-
                            Default maximum message size in bytes. 0 = no org-wide default.
                    secretsManager:
                      type: object
                      default: {}
                      description: >-
                        Default SecretsManager-family governance fields. Cascade levels 8-9 in
                        the ten-level priority chain (ADR-015 §5.3). Applied only when no
                        higher-priority source sets the field.
                      properties:
                        kmsKeyID:
                          type: string
                          description: >-
                            Default KMS key ID, ARN, or alias for Secrets Manager secrets.
                            Empty string = no org-wide default encryption key.
                    sns:
                      type: object
                      default: {}
                      description: >-
                        Default SNS-family governance fields. Cascade levels 8-9 in the
                        ten-level priority chain (ADR-015 §5.3). Applied only when no
                        higher-priority source sets the field.
                      properties:
                        kmsMasterKeyId:
                          type: string
                          description: >-
                            Default KMS key ID or ARN for SNS topic SSE.
                            Empty string = no org-wide default encryption key.
                        signatureVersion:
                          type: string
                          description: >-
                            Default SNS message signature version ("1" or "2").
                            Empty string = no org-wide default.
                        tracingConfig:
                          type: string
                          description: >-
                            Default SNS X-Ray active tracing mode ("Active" or "PassThrough").
                            Empty string = no org-wide default.
                    dynamodb:
                      type: object
                      default: {}
                      description: >-
                        Default DynamoDB-family governance fields. Cascade levels 8-9 (ADR-015 §5.3).
                        Applied only when no higher-priority source sets the field.
                      properties:
                        encryptionEnabled:
                          type: boolean
                          description: >-
                            Default SSE setting. Omit (nil) = no default; false = disabled by default.
                        kmsMasterKeyId:
                          type: string
                          description: Default KMS key ARN/ID. Empty = no default.
                        deletionProtectionEnabled:
                          type: boolean
                          description: Default deletion-protection. Omit (nil) = no default.
                        pointInTimeRecoveryEnabled:
                          type: boolean
                          description: Default PITR. Omit (nil) = no default.
                        billingMode:
                          type: string
                          enum: ["", "PROVISIONED", "PAY_PER_REQUEST"]
                          description: Default billing mode. Empty = no default.
                        tableClass:
                          type: string
                          enum: ["", "STANDARD", "STANDARD_INFREQUENT_ACCESS"]
                          description: Default table class. Empty = no default.
                        contributorInsights:
                          type: string
                          enum: ["", "ENABLE", "DISABLE"]
                          description: Default Contributor Insights action. Empty = no default.
                    eventbridge:
                      type: object
                      default: {}
                      description: >-
                        Default EventBridge-family governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                      properties:
                        archiveRetentionDays:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: >-
                            Default archive retention in days. 0 = indefinite retention (no default
                            enforced). Applied when no mandatory source sets this field.
                    elb:
                      type: object
                      default: {}
                      description: >-
                        Default ELB-family governance fields. Cascade levels 8-9 (ADR-015 §5.3).
                        Only the three cross-type ELB boolean fields are governed here; fields
                        specific to ELBConfig (accessLogsS3Bucket, crossZoneEnabled, etc.) are
                        not in KropathConfig. Applied only when no higher-priority source sets
                        the field.
                      properties:
                        deletionProtection:
                          type: boolean
                          default: false
                          description: >-
                            Default deletion-protection preference. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                        accessLogsEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Default access-logging preference. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                        internalOnly:
                          type: boolean
                          default: false
                          description: >-
                            Default scheme preference. false = both internal and internet-facing
                            are allowed. Applied when no mandatory source sets this field.
                    cloudwatchlogs:
                      type: object
                      default: {}
                      description: >-
                        Default CloudWatch Logs-family governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                      properties:
                        kmsKeyId:
                          type: string
                          description: >-
                            Default KMS key ARN for log group encryption. Empty string = no
                            encryption default. Applied when no mandatory source sets this field.
                        retentionDays:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: >-
                            Default retention period in days. 0 = no default retention enforced.
                            Applied when no mandatory source sets this field.
                    cloudtrail:
                      type: object
                      default: {}
                      description: >-
                        Default CloudTrail-family governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                      properties:
                        isMultiRegionTrail:
                          type: boolean
                          description: >-
                            Default multi-region trail setting. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                        enableLogFileValidation:
                          type: boolean
                          description: >-
                            Default log file integrity validation setting. false = no default
                            enforcement. Applied when no mandatory source sets this field.
                        retentionPeriod:
                          type: integer
                          format: int64
                          minimum: 0
                          description: >-
                            Default event data store retention period in days. 0 = no default.
                            Applied when no mandatory source sets this field.
                    cloudwatch:
                      type: object
                      default: {}
                      description: >-
                        Default CloudWatch-family governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                      properties:
                        actionsEnabled:
                          type: boolean
                          nullable: true
                          description: >-
                            Default CloudWatch alarm actions enabled setting. null = no default.
                            Applied when no mandatory source sets this field.
                        treatMissingData:
                          type: string
                          description: >-
                            Default treatment of missing data in alarms. Empty string = no default.
                            Applied when no mandatory source sets this field.
                        outputFormat:
                          type: string
                          description: >-
                            Default CloudWatch metric stream output format. Empty string = no
                            default. Applied when no mandatory source sets this field.
                        tags:
                          type: object
                          default: {}
                          description: >-
                            Default CloudWatch resource tags. Merged additively; mandatory
                            sources win on key conflict.
                          additionalProperties:
                            type: string
                    cloudfront:
                      type: object
                      default: {}
                      description: >-
                        Default CloudFront-family governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                      properties:
                        viewerProtocolPolicy:
                          type: string
                          description: >-
                            Default viewer protocol policy. Empty string = no default enforcement.
                            Applied when no mandatory source sets this field.
                        minimumProtocolVersion:
                          type: string
                          description: >-
                            Default minimum TLS version. Empty string = no default enforcement.
                            Applied when no mandatory source sets this field.
                        webACLRequired:
                          type: boolean
                          default: false
                          description: >-
                            Default WAF requirement preference. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                        loggingEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Default access-logging preference. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                    rds:
                      type: object
                      default: {}
                      description: >-
                        Default RDS-family governance fields. Cascade levels 8-9 (ADR-015 §5.3).
                        Applied only when no higher-priority source sets the field.
                        Cluster-only fields are not in KropathConfig — use RDSConfig for those.
                      properties:
                        storageEncrypted:
                          type: boolean
                          description: >-
                            Default encryption-at-rest setting. Omit (nil) = no default;
                            false = disabled by default. No default — nil detection required.
                        kmsKeyID:
                          type: string
                          description: Default KMS key ARN for RDS encryption. Empty = no default.
                        deletionProtection:
                          type: boolean
                          description: Default deletion-protection setting. Omit (nil) = no default.
                        backupRetentionPeriod:
                          type: integer
                          format: int64
                          default: 0
                          description: >-
                            Default backup retention in days. 0 = no automated backups by default.
                        multiAZ:
                          type: boolean
                          description: Default Multi-AZ setting. Omit (nil) = no default.
                        publiclyAccessible:
                          type: boolean
                          description: Default public-accessibility setting. Omit (nil) = no default.
                        storageType:
                          type: string
                          description: Default storage type. Empty = no default.
                        autoMinorVersionUpgrade:
                          type: boolean
                          description: Default auto-minor-version-upgrade setting. Omit (nil) = no default.
                        copyTagsToSnapshot:
                          type: boolean
                          description: Default copy-tags-to-snapshot setting. Omit (nil) = no default.
                        performanceInsightsEnabled:
                          type: boolean
                          description: Default Performance Insights setting. Omit (nil) = no default.
                        enableIAMDatabaseAuthentication:
                          type: boolean
                          description: Default IAM database authentication. Omit (nil) = no default.
                        manageMasterUserPassword:
                          type: boolean
                          description: Default Secrets Manager password management. Omit (nil) = no default.
                        namingTemplate:
                          type: string
                          description: Default RDS naming template. Empty = no default.
                        tags:
                          type: object
                          default: {}
                          description: Default cloud resource tags for RDS resources.
                          additionalProperties:
                            type: string
                    autoscaling:
                      type: object
                      default: {}
                      description: >-
                        Default Auto Scaling-family governance fields. Cascade levels 8-9 (ADR-015 §5.3).
                        Applied only when no higher-priority source sets the field.
                      properties:
                        newInstancesProtectedFromScaleIn:
                          type: boolean
                          default: false
                          description: >-
                            Default scale-in protection setting. false = no default.
                        capacityRebalance:
                          type: boolean
                          default: false
                          description: >-
                            Default Spot capacity rebalancing setting. false = no default.
                    lambda:
                      type: object
                      default: {}
                      description: >-
                        Lambda-family default governance fields. Applied when no higher-priority
                        source sets the field. Cascade levels 8-9 (ADR-015 §5.3).
                      properties:
                        runtime:
                          type: string
                          description: Default Lambda runtime. Empty = no default runtime enforced.
                        memorySize:
                          type: integer
                          format: int64
                          default: 0
                          description: Default memory in MB. 0 = no default enforced.
                        timeout:
                          type: integer
                          format: int64
                          default: 0
                          description: Default execution timeout in seconds. 0 = no default enforced.
                        tracingMode:
                          type: string
                          description: Default AWS X-Ray tracing mode. Empty = no default enforced.
                        kmsKeyArn:
                          type: string
                          description: Default KMS key ARN for environment variable encryption. Empty = no default enforced.
                    ec2:
                      type: object
                      default: {}
                      description: >-
                        Default EC2-family governance fields. Cascade levels 8-9 (ADR-015 §5.3).
                        Only the three cross-type EC2 boolean fields are governed here.
                        Applied only when no higher-priority source sets the field.
                      properties:
                        flowLogsRequired:
                          type: boolean
                          default: false
                          description: >-
                            Default VPC Flow Logs enforcement. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                        imdsv2Required:
                          type: boolean
                          default: false
                          description: >-
                            Default IMDSv2-only enforcement. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                        ebsEncryptionRequired:
                          type: boolean
                          default: false
                          description: >-
                            Default EBS encryption enforcement. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                    ecs:
                      type: object
                      default: {}
                      description: >-
                        ECS-family default governance fields. Applied when no higher-priority
                        source sets the field. Cascade levels 8-9 (ADR-015 §5.3).
                        Only the two cross-type fields are governed here.
                      properties:
                        containerInsights:
                          type: boolean
                          default: false
                          description: >-
                            Default Container Insights enforcement. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                        defaultLaunchType:
                          type: string
                          description: >-
                            Default ECS launch type (EC2 | FARGATE). Empty = no default enforced.
                            Applied when no mandatory or ECSConfig source sets this field.
                    apigatewayv2:
                      type: object
                      default: {}
                      description: >-
                        API Gateway V2-family default governance fields. Applied when no
                        higher-priority source sets the field. Cascade levels 8-9 (ADR-015 §5.3).
                        Only the three cross-type fields are governed here.
                      properties:
                        corsEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Default CORS enforcement toggle. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                        disableExecuteApiEndpoint:
                          type: boolean
                          default: false
                          description: >-
                            Default execute-api endpoint disable toggle. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                        minimumTlsVersion:
                          type: string
                          description: >-
                            Default minimum TLS version for custom domain names. Empty = no default.
                            Applied when no mandatory or ApiGatewayV2Config source sets this field.
                    elasticache:
                      type: object
                      default: {}
                      description: >-
                        Default ElastiCache-family governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                      properties:
                        atRestEncryptionEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Default encryption-at-rest preference. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                        transitEncryptionEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Default TLS in-transit preference. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                    ecr:
                      type: object
                      default: {}
                      description: >-
                        ECR-family default governance fields. Applied when no higher-priority
                        source sets the field. Cascade levels 8-9 (ADR-015 §5.3).
                        Only the three cross-type fields are governed here.
                      properties:
                        imageTagMutability:
                          type: string
                          description: >-
                            Default image tag mutability (IMMUTABLE | MUTABLE). Empty = no default.
                            Applied when no mandatory or ECRConfig source sets this field.
                        encryptionType:
                          type: string
                          description: >-
                            Default repository encryption type (AES256 | KMS). Empty = no default.
                            Applied when no mandatory or ECRConfig source sets this field.
                        lifecyclePolicy:
                          type: string
                          description: >-
                            Default lifecycle policy JSON template. Empty = no default.
                            Applied when no mandatory or ECRConfig source sets this field.
                    stepfunctions:
                      type: object
                      default: {}
                      description: >-
                        Default Step Functions-family governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                        Only loggingLevel is governed at KropathConfig level.
                      properties:
                        loggingLevel:
                          type: string
                          description: >-
                            Default execution logging level. "" = no org-wide default.
                            Valid values: "" | "ALL" | "ERROR" | "FATAL" | "OFF".
                            Applied when no mandatory or StepFunctionsConfig source sets this field.
                        tags:
                          type: object
                          default: {}
                          description: Default Step Functions cloud resource tags.
                          additionalProperties:
                            type: string
                    managedprometheus:
                      type: object
                      default: {}
                      description: >-
                        Managed Prometheus-family default governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                      properties:
                        alias:
                          type: string
                          description: >-
                            Default workspace alias. Empty string = no alias default.
                            Applied when no mandatory source sets this field.
                        logGroupARN:
                          type: string
                          description: >-
                            Default CloudWatch log group ARN for AMP workspace vended logs.
                            Empty string = no log destination default.
                            Applied when no mandatory source sets this field.
                    memorydb:
                      type: object
                      default: {}
                      description: >-
                        MemoryDB-family default governance fields. Cascade levels 8-9 in the
                        ten-level priority chain (ADR-015 §5.3). Applied only when no
                        higher-priority source sets the field.
                      properties:
                        tlsEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Default TLS in-transit preference. false = no default enforcement.
                            Applied when no mandatory source sets this field.
                        allowedNodeTypes:
                          type: array
                          description: >-
                            Default node type allowlist. [] = no restriction.
                            Applied when no mandatory source sets allowedNodeTypes.
                          items:
                            type: string
                        snapshotRetentionLimit:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: >-
                            Default snapshot retention floor in days. 0 = no default enforcement.
                            Applied when no mandatory source sets snapshotRetentionLimit.
                    mq:
                      type: object
                      default: {}
                      description: >-
                        MQ-family default governance fields. Cascade levels 8-9 in the
                        ten-level priority chain (ADR-015 §5.3). Applied only when no
                        higher-priority source sets the field.
                      properties:
                        engineType:
                          type: string
                          description: >-
                            Default broker engine type. "" = no org-wide default.
                            Valid: "" | "ACTIVEMQ" | "RABBITMQ".
                        authenticationStrategy:
                          type: string
                          description: >-
                            Default authentication strategy. "" = no org-wide default.
                            Valid: "" | "simple" | "ldap".
                        deploymentMode:
                          type: string
                          description: >-
                            Default broker deployment mode. "" = no org-wide default.
                            Valid: "" | "SINGLE_INSTANCE" | "ACTIVE_STANDBY_MULTI_AZ" |
                            "CLUSTER_MULTI_AZ".
                        publiclyAccessible:
                          type: boolean
                          description: >-
                            Default public accessibility preference. false = no org-wide default.
                            Applied when no mandatory source sets this field.
                        autoMinorVersionUpgrade:
                          type: boolean
                          description: >-
                            Default automatic minor version upgrade preference. false = no org-wide default.
                            Applied when no mandatory source sets this field.
                        logsGeneral:
                          type: boolean
                          description: >-
                            Default general logging preference. false = no org-wide default.
                            Applied when no mandatory source sets this field.
                        logsAudit:
                          type: boolean
                          description: >-
                            Default audit logging preference. false = no org-wide default.
                            Applied when no mandatory source sets this field.
                        encryptionUseAWSOwnedKey:
                          type: boolean
                          description: >-
                            Default encryption key preference. false = no org-wide default.
                            Applied when no mandatory source sets this field.
                        storageType:
                          type: string
                          description: >-
                            Default storage type for ActiveMQ brokers. "" = no org-wide default.
                            Valid: "" | "efs" | "io1".
                    msk:
                      type: object
                      default: {}
                      description: >-
                        Default MSK-family governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                        Only the five KropathConfig-scoped fields apply here.
                      properties:
                        kafkaVersion:
                          type: string
                          description: >-
                            Default Kafka version. "" = no org-wide default.
                            Applied when no mandatory or MSKConfig source sets this field.
                        enhancedMonitoring:
                          type: string
                          description: >-
                            Default CloudWatch monitoring level. "" = no org-wide default.
                            Valid: "" | "DEFAULT" | "PER_BROKER" |
                            "PER_TOPIC_PER_BROKER" | "PER_TOPIC_PER_PARTITION".
                        encryptionInTransitClientBroker:
                          type: string
                          description: >-
                            Default client-broker encryption mode. "" = no org-wide default.
                        encryptionInTransitInCluster:
                          type: string
                          description: >-
                            Default inter-broker encryption (three-state string).
                            "" = no org-wide default. Valid: "" | "true" | "false".
                        encryptionAtRestKmsKeyId:
                          type: string
                          description: >-
                            Default KMS key ARN for EBS encryption. "" = no org-wide default.
                        tags:
                          type: object
                          default: {}
                          description: Default MSK cloud resource tags.
                          additionalProperties:
                            type: string
                    certificateManager:
                      type: object
                      default: {}
                      description: >-
                        Certificate Manager family governance fields applied at the org-wide
                        defaults tier. Cascade levels 8-9 in the ten-level priority chain
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                      properties:
                        keyAlgorithm:
                          type: string
                          description: >-
                            Default key algorithm for all ACM certificates and private CAs.
                            Empty string = no org-wide default. Valid: RSA_2048 | EC_prime256v1 |
                            EC_secp384r1.
                        certificateTransparencyLogging:
                          type: string
                          description: >-
                            Default CT logging preference. Empty string = no org-wide default.
                            Valid: ENABLED | DISABLED.
                    documentdb:
                      type: object
                      default: {}
                      description: >-
                        Default DocumentDB-family governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                        Only the four KropathConfig-scoped fields apply here.
                      properties:
                        storageEncrypted:
                          type: boolean
                          default: false
                          description: >-
                            Default encryption-at-rest for DocumentDB clusters. false = no org-wide default.
                            Applied when no mandatory source sets storageEncrypted.
                        deletionProtection:
                          type: boolean
                          default: false
                          description: >-
                            Default deletion-protection for DocumentDB clusters. false = no org-wide default.
                            Applied when no mandatory source sets deletionProtection.
                        allowedInstanceClasses:
                          type: array
                          description: >-
                            Default DocumentDB instance class allowlist. [] = no org-wide default.
                            Applied when no mandatory source sets allowedInstanceClasses.
                          items:
                            type: string
                        backupRetentionPeriod:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: >-
                            Default backup retention in days. 0 = no org-wide default.
                            Applied when no mandatory source sets backupRetentionPeriod.
                    opensearch:
                      type: object
                      default: {}
                      description: >-
                        Default OpenSearch-family governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                        Only the four KropathConfig-scoped fields apply here.
                      properties:
                        encryptionAtRestEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Default encryption-at-rest for OpenSearch domains. false = no org-wide default.
                            Applied when no mandatory source sets encryptionAtRestEnabled.
                        nodeToNodeEncryptionEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Default node-to-node encryption for OpenSearch domains. false = no org-wide default.
                            Applied when no mandatory source sets nodeToNodeEncryptionEnabled.
                        enforceHTTPS:
                          type: boolean
                          default: false
                          description: >-
                            Default HTTPS enforcement for OpenSearch domains. false = no org-wide default.
                            Applied when no mandatory source sets enforceHTTPS.
                        tlsSecurityPolicy:
                          type: string
                          description: >-
                            Default minimum TLS security policy for OpenSearch domains. Empty string = no org-wide default.
                            Applied when no mandatory source sets tlsSecurityPolicy.
                    dsql:
                      type: object
                      default: {}
                      description: >-
                        Default Aurora DSQL governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                      properties:
                        deletionProtectionEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Default deletion-protection setting for Aurora DSQL clusters. false = no org-wide default.
                            Applied when no mandatory source sets deletionProtectionEnabled.
                    route53:
                      type: object
                      default: {}
                      description: >-
                        Default Route53 governance fields. Cascade levels 8-9
                        (ADR-015 §5.3). Applied only when no higher-priority source sets the field.
                      properties:
                        defaultTTL:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          description: >-
                            Org-wide default TTL in seconds for DNS records. 0 = no org-wide default.
                            Applied when no mandatory source sets defaultTTL.
                        healthCheckRequestInterval:
                          type: integer
                          format: int64
                          default: 0
                          description: >-
                            Default health check polling interval in seconds. 0 = no org-wide default.
                            Valid non-zero values: 10 or 30.
                        healthCheckFailureThreshold:
                          type: integer
                          format: int64
                          default: 0
                          minimum: 0
                          maximum: 10
                          description: >-
                            Default consecutive-failure threshold. 0 = no org-wide default.
                            Valid non-zero range: 1–10.
                        resolverEndpointType:
                          type: string
                          description: >-
                            Default IP address family for Resolver endpoints. Empty = no org-wide default.
                            Valid values: IPV4, IPV6, DUALSTACK.
                    ssm:
                      type: object
                      default: {}
                      description: >-
                        Default SSM-family governance fields. Cascade levels 8-9 in the
                        ten-level priority chain (ADR-015 §5.3). Applied only when no
                        higher-priority source sets the field. Only parameterType, parameterTier,
                        keyID, and allowedDocumentTypes apply at this level.
                      properties:
                        parameterType:
                          type: string
                          description: >-
                            Default parameter type (String | StringList | SecureString).
                            Empty string = no org-wide default.
                            Applied when no mandatory source sets parameterType.
                        parameterTier:
                          type: string
                          description: >-
                            Default parameter tier (Standard | Advanced | Intelligent-Tiering).
                            Empty string = no org-wide default.
                            Applied when no mandatory source sets parameterTier.
                        keyID:
                          type: string
                          description: >-
                            Default KMS key ARN, ID, or alias for SecureString encryption.
                            Empty string = no org-wide default.
                            Applied when no mandatory source sets keyID.
                        allowedDocumentTypes:
                          type: array
                          description: >-
                            Default allowed SSM document type allowlist. [] = no org-wide default.
                            Applied when no mandatory source sets allowedDocumentTypes.
                          items:
                            type: string
                    cognito:
                      type: object
                      default: {}
                      description: >-
                        Cognito-family governance fields applied at the org-wide defaults tier.
                        Cascade levels 8-9 in the ten-level priority chain (ADR-015 §5.3).
                        Weakest defaults source; CognitoConfig defaults (levels 6-7) override these.
                      properties:
                        mfaConfiguration:
                          type: string
                          description: >-
                            Org-wide default MFA level ("OFF", "OPTIONAL", "ON").
                            Empty string = no default; instances can override.
                        deletionProtection:
                          type: string
                          description: >-
                            Org-wide default deletion protection state ("ACTIVE", "INACTIVE").
                            Empty string = no default.
                        advancedSecurityMode:
                          type: string
                          description: >-
                            Org-wide default advanced security mode ("OFF", "AUDIT", "ENFORCED").
                            Empty string = no default.
                        passwordPolicy:
                          type: object
                          default: {}
                          description: Default Cognito password policy sub-fields.
                          properties:
                            minimumLength:
                              type: integer
                              format: int64
                              default: 0
                              description: Default minimum password length. 0 = no default; valid range 6-99.
                            requireLowercase:
                              type: boolean
                              description: Default require lowercase. Omit = no default.
                            requireNumbers:
                              type: boolean
                              description: Default require digits. Omit = no default.
                            requireSymbols:
                              type: boolean
                              description: Default require symbols. Omit = no default.
                            requireUppercase:
                              type: boolean
                              description: Default require uppercase. Omit = no default.
                            temporaryPasswordValidityDays:
                              type: integer
                              format: int64
                              default: 0
                              description: Default temporary password expiry in days. 0 = no default; valid range 1-365.
                    keyspaces:
                      type: object
                      default: {}
                      description: >-
                        Keyspaces-family governance fields applied at the org-wide defaults tier.
                        Cascade levels 8-9 in the ten-level priority chain (ADR-015 §5.3).
                        Weakest defaults source; KeyspacesConfig defaults (levels 6-7) override these.
                      properties:
                        replicationStrategy:
                          type: string
                          description: >-
                            Org-wide default replication strategy ("SINGLE_REGION" or "MULTI_REGION").
                            Empty string = no default; instances can override.
                        throughputMode:
                          type: string
                          description: >-
                            Org-wide default throughput mode ("PAY_PER_REQUEST" or "PROVISIONED").
                            Empty string = no default.
                        encryptionType:
                          type: string
                          description: >-
                            Org-wide default encryption type
                            ("AWS_OWNED_KMS_KEY" or "CUSTOMER_MANAGED_KMS_KEY").
                            Empty string = no default.
                        pointInTimeRecovery:
                          type: boolean
                          default: false
                          description: >-
                            Org-wide default point-in-time recovery setting for Keyspaces tables.
                            false = no default.
                        ttlEnabled:
                          type: boolean
                          default: false
                          description: >-
                            Org-wide default TTL enabled setting for Keyspaces tables.
                            false = no default.
                    bedrock:
                      type: object
                      default: {}
                      description: >-
                        Bedrock-family governance fields applied at the org-wide defaults tier.
                        Cascade levels 8-9 in the ten-level priority chain (ADR-015 §5.3).
                        Weakest defaults source; BedrockConfig defaults (levels 6-7) override these.
                        Applied only when no higher-priority source sets the field.
                      properties:
                        guardrailIdentifier:
                          type: string
                          description: >-
                            Org-wide default Bedrock guardrail identifier. Empty string = no default.
                            Applied when no mandatory source sets this field.
                        guardrailVersion:
                          type: string
                          description: >-
                            Org-wide default Bedrock guardrail version. Empty string = no default.
                            Applied when no mandatory source sets this field.
                        allowedModels:
                          type: array
                          description: >-
                            Org-wide default model allowlist. [] = no restriction; non-empty = default
                            allowlist applied when no mandatory source sets this field.
                          items:
                            type: string
                        tags:
                          type: object
                          default: {}
                          additionalProperties:
                            type: string
                          description: Cloud resource tags merged into all Bedrock resources org-wide (defaults tier).
                    sagemaker:
                      type: object
                      description: >-
                        Org-wide default SageMaker governance fields. Applied when no higher-priority source
                        sets the field. Zero-value = no default at this level.
                      properties:
                        instanceType:
                          type: string
                          description: Org-wide default SageMaker instance type. Empty = no default.
                        kmsKeyId:
                          type: string
                          description: Org-wide default KMS key for SageMaker storage encryption. Empty = no default.
                        enableNetworkIsolation:
                          type: boolean
                          description: Org-wide default network isolation setting. false = no default (zero-value sentinel).
                        enableInterContainerTrafficEncryption:
                          type: boolean
                          description: Org-wide default inter-container traffic encryption. false = no default (zero-value sentinel).
                    pipes:
                      type: object
                      default: {}
                      description: >-
                        Org-wide default EventBridge Pipes governance fields. Applied when no higher-priority
                        source sets the field. Zero-value = no default at this level.
                      properties:
                        desiredState:
                          type: string
                          default: ""
                          description: Org-wide default desired state for EventBridge Pipes (RUNNING | STOPPED). Empty = no default.
                    codeartifact:
                      type: object
                      default: {}
                      description: >-
                        Org-wide default CodeArtifact governance fields. Applied when no higher-priority
                        source sets the field. Zero-value = no default at this level.
                      properties:
                        encryptionKey:
                          type: string
                          default: ""
                          description: Org-wide default KMS key ARN for CodeArtifact domain encryption. Empty = no default.
                    mwaa:
                      type: object
                      default: {}
                      description: >-
                        Org-wide default MWAA governance fields. Applied when no higher-priority source
                        sets the field. Zero-value = no default at this level.
                      properties:
                        airflowVersion:
                          type: string
                          default: ""
                          description: Org-wide default Airflow version. Empty = no default.
                        environmentClass:
                          type: string
                          default: ""
                          description: Org-wide default environment class. Empty = no default.
                        webserverAccessMode:
                          type: string
                          default: ""
                          description: Org-wide default web server access mode. Empty = no default.
                        endpointManagement:
                          type: string
                          default: ""
                          description: Org-wide default VPC endpoint management model. Empty = no default.
                        kmsKeyARN:
                          type: string
                          default: ""
                          description: Org-wide default KMS key ARN for MWAA encryption. Empty = no default.
                        dagProcessingLogsEnabled:
                          type: boolean
                          description: Org-wide default DAG processing logging state. Absent (nil) = no default.
                        dagProcessingLogsLevel:
                          type: string
                          default: ""
                          description: Org-wide default DAG processing log level. Empty = no default.
                        schedulerLogsEnabled:
                          type: boolean
                          description: Org-wide default scheduler logging state. Absent (nil) = no default.
                        schedulerLogsLevel:
                          type: string
                          default: ""
                          description: Org-wide default scheduler log level. Empty = no default.
                        taskLogsEnabled:
                          type: boolean
                          description: Org-wide default task logging state. Absent (nil) = no default.
                        taskLogsLevel:
                          type: string
                          default: ""
                          description: Org-wide default task log level. Empty = no default.
                        webserverLogsEnabled:
                          type: boolean
                          description: Org-wide default web server logging state. Absent (nil) = no default.
                        webserverLogsLevel:
                          type: string
                          default: ""
                          description: Org-wide default web server log level. Empty = no default.
                        workerLogsEnabled:
                          type: boolean
                          description: Org-wide default worker logging state. Absent (nil) = no default.
                        workerLogsLevel:
                          type: string
                          default: ""
                          description: Org-wide default worker log level. Empty = no default.
                        maxWorkers:
                          type: integer
                          format: int64
                          default: 0
                          description: Org-wide default maximum worker count. 0 = no default.
                        minWorkers:
                          type: integer
                          format: int64
                          default: 0
                          description: Org-wide default minimum worker count. 0 = no default.
                        maxWebservers:
                          type: integer
                          format: int64
                          default: 0
                          description: Org-wide default maximum web server count. 0 = no default.
                        minWebservers:
                          type: integer
                          format: int64
                          default: 0
                          description: Org-wide default minimum web server count. 0 = no default.
                        schedulers:
                          type: integer
                          format: int64
                          default: 0
                          description: Org-wide default scheduler count. 0 = no default.
                        weeklyMaintenanceWindowStart:
                          type: string
                          default: ""
                          description: Org-wide default maintenance window start. Empty = MWAA chooses.
                        namingTemplate:
                          type: string
                          default: ""
                          description: Org-wide default naming template for MWAA environments. Empty = no default.
                    ram:
                      type: object
                      default: {}
                      description: >-
                        RAM-family governance fields applied at the org-wide defaults tier.
                        Cascade levels 8-9 in the ten-level priority chain (ADR-015 §5.3).
                        Weakest defaults source; RAMConfig defaults (levels 6-7) override these.
                      properties:
                        allowExternalPrincipals:
                          type: boolean
                          nullable: true
                          description: >-
                            Org-wide default external-principals policy. nil = no default; false =
                            default-disallow; true = default-allow. Overridden by mandatory tier
                            or RAMConfig.
                        allowedResourceTypes:
                          type: array
                          description: >-
                            Org-wide default allowlist of RAM-shareable resource types. Empty = no
                            default restriction.
                          items:
                            type: string
                    quicksight:
                      type: object
                      default: {}
                      description: >-
                        Default QuickSight-family governance fields.
                        Cascade levels 8-9 in the ten-level priority chain (ADR-015 §5.3).
                        Applied only when no higher-priority source sets the field.
                      properties:
                        importMode:
                          type: string
                          description: >-
                            Default dataset import mode org-wide. Empty = no default.
                            Allowed values: SPICE, DIRECT_QUERY.
                            Cannot be set together with mandatory.quicksight.importMode.

                aws:
                  type: object
                  default: {}
                  description: >-
                    AWS provider identity fields. Controller copies these into
                    effectiveConfig.aws.* on every namespaced ResourceConfig CR (ADR-015 §5.4).
                  properties:
                    accountId:
                      type: string
                      default: ""
                      description: AWS account ID (12-digit string).
                    region:
                      type: string
                      default: ""
                      description: AWS region identifier (e.g. us-east-1).
            status:
              type: object
              properties:
                conditions:
                  type: array
                  description: Conditions represent the latest available observations of an object state.
                  x-kubernetes-list-type: map
                  x-kubernetes-list-map-keys:
                    - type
                  items:
                    type: object
                    required:
                      - type
                      - status
                    properties:
                      type:
                        type: string
                      status:
                        type: string
                        enum: ["True", "False", "Unknown"]
                      observedGeneration:
                        type: integer
                        format: int64
                      lastTransitionTime:
                        type: string
                        format: date-time
                      reason:
                        type: string
                      message:
                        type: string
                observedGeneration:
                  type: integer
                  format: int64
                  description: Generation of the spec most recently reconciled by kropath-controller.
                syncedTimestamp:
                  type: string
                  description: RFC3339 timestamp of the last successful reconcile.
