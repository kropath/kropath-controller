apiVersion: aws.kropath.run/v1alpha1
kind: KropathConfig
metadata:
  name: baseline
  namespace: ecr-global-ns
spec:
  mandatory: {}
  # spec.aws was removed from the schema entirely in KRO-1241 (ADR-019,
  # completing KRO-1129); the field cannot be set on this object at all
  # anymore. Kept name/namespace matching AC-16's original intent -- proves
  # effectiveConfig.aws is resolved solely from the resource namespace's
  # owner-account-id/default-region annotations, never from KropathConfig
  # (spec AC-2, controller-account-region-placement.md).
