apiVersion: aws.kropath.run/v1alpha1
kind: ECRConfig
metadata:
  name: general-policy
  namespace: ecr-app-ns
status:
  # accountId/region come from the ecr-app-ns namespace annotations, not from
  # KropathConfig.spec.aws above -- proves the field is inert (spec AC-2).
  effectiveConfig:
    aws:
      accountId: "123456789012"
      region: us-east-1
