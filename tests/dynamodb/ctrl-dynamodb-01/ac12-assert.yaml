apiVersion: aws.kropath.run/v1alpha1
kind: DynamoDBConfig
metadata:
  name: ac12-general-table
  namespace: dynamodb
status:
  effectiveConfig:
    defaults:
      namingTemplate: "{namespace}-{name}"
