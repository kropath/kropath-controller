apiVersion: aws.kropath.run/v1alpha1
kind: APIGatewayConfig
metadata:
  name: general-policy
  namespace: kro-system
spec:
  mandatory:
    tags:
      api-type: rest
  defaults:
    tags:
      team: platform
