# Global EFSConfig (kro-system) — sets mandatory.backupEnabled=true (level 3).
apiVersion: aws.kropath.run/v1alpha1
kind: EFSConfig
metadata:
  name: general-storage
  namespace: kro-system
spec:
  mandatory:
    backupEnabled: true
