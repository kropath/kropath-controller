# AC-C7: effectiveConfig.defaults.replicationOverwriteProtection propagates.
apiVersion: aws.kropath.run/v1alpha1
kind: EFSConfig
metadata:
  name: general-storage
  namespace: efs
status:
  effectiveConfig:
    defaults:
      replicationOverwriteProtection: ENABLED
