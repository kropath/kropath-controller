# Global EFSConfig — sets mandatory.syncedLabels (level 3).
apiVersion: aws.kropath.run/v1alpha1
kind: EFSConfig
metadata:
  name: general-storage
  namespace: kro-system
spec:
  mandatory:
    syncedLabels:
      data-class: sensitive
