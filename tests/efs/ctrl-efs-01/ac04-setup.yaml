# Local EFSConfig (namespace=efs) — sets defaults.performanceMode (level 6).
apiVersion: aws.kropath.run/v1alpha1
kind: EFSConfig
metadata:
  name: ac4-general-storage
  namespace: efs
spec:
  defaults:
    performanceMode: generalPurpose
