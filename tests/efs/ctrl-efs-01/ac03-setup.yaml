# Local EFSConfig (namespace=efs) — sets defaults.encrypted=false (level 6).
# Verifies that explicit false is preserved (not treated as nil).
apiVersion: aws.kropath.run/v1alpha1
kind: EFSConfig
metadata:
  name: ac3-general-storage
  namespace: efs
spec:
  defaults:
    encrypted: false
