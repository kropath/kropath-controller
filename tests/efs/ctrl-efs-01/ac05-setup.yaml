# Local EFSConfig (namespace=efs) — sets defaults.throughputMode (level 6).
apiVersion: aws.kropath.run/v1alpha1
kind: EFSConfig
metadata:
  name: ac5-general-storage
  namespace: efs
spec:
  defaults:
    throughputMode: elastic
