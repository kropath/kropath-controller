# Assert AC-T02-8: defaults.namingTemplate from global SageMakerConfig defaults.
apiVersion: aws.kropath.run/v1alpha1
kind: SageMakerConfig
metadata:
  name: ac08-sm-policy
  namespace: payments-prod
status:
  effectiveConfig:
    defaults:
      namingTemplate: "{namespace}-{name}"
