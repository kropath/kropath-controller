# Assert AC-11: defaults.namingTemplate="{namespace}-{name}" from level-7 globalSQSConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: SQSConfig
metadata:
  name: ac11-general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    defaults:
      namingTemplate: "{namespace}-{name}"
