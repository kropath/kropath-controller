# Assert AC-12: mandatory.syncedLabels from level-3 globalSQSConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: SQSConfig
metadata:
  name: ac12-general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    mandatory:
      syncedLabels:
        data-class: internal
