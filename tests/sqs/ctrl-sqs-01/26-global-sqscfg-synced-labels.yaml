# AC-12: globalSQSConfig with mandatory.syncedLabels={data-class: internal} (level 3).
apiVersion: aws.kropath.run/v1alpha1
kind: SQSConfig
metadata:
  name: general-policy
  namespace: kro-system
spec:
  mandatory:
    syncedLabels:
      data-class: internal
  defaults: {}
