# Assert AC-1: mandatory.encryptionType="kms" from level-1 globalKropathConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: SQSConfig
metadata:
  name: ac1-general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    mandatory:
      encryptionType: "kms"
