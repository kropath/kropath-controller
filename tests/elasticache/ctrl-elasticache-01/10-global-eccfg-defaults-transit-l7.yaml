# AC-5: globalElastiCacheConfig with defaults.transitEncryptionEnabled=true (level 7).
apiVersion: aws.kropath.run/v1alpha1
kind: ElastiCacheConfig
metadata:
  name: general-policy
  namespace: kro-system
spec:
  mandatory: {}
  defaults:
    transitEncryptionEnabled: true
