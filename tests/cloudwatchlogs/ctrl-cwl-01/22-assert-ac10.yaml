# Assert AC-10: effCfg.mandatory.syncedLabels contains data-class: internal from globalCWLConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: CloudWatchLogsConfig
metadata:
  name: general-policy
  namespace: logging-prod
status:
  effectiveConfig:
    mandatory:
      syncedLabels:
        data-class: internal
