# Assert AC-C3: defaults.capacityRebalance=true; mandatory.capacityRebalance is absent (false).
apiVersion: aws.kropath.run/v1alpha1
kind: AutoScalingConfig
metadata:
  name: general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    defaults:
      capacityRebalance: true
