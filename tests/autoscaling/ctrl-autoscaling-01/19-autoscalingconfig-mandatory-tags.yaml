# AC-C8: localAutoScalingConfig with mandatory.tags (level 4).
# cost-centre is also set by KropathConfig; KropathConfig must win.
apiVersion: aws.kropath.run/v1alpha1
kind: AutoScalingConfig
metadata:
  name: general-policy
  namespace: payments-prod
spec:
  mandatory:
    tags:
      cost-centre: app
      asg-type: spot
  defaults: {}
