# Assert AC-C7: mandatory.namingTemplate="{namespace}-{name}" from level-3 globalAutoScalingConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: AutoScalingConfig
metadata:
  name: general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    mandatory:
      namingTemplate: "{namespace}-{name}"
