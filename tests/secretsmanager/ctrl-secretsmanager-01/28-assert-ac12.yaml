# Assert AC-12: mandatory.tags is union of KPC tags and SMConfig tags.
apiVersion: aws.kropath.run/v1alpha1
kind: SecretsManagerConfig
metadata:
  name: general-policy
  namespace: secrets-prod
status:
  effectiveConfig:
    mandatory:
      tags:
        cost-centre: "security"
        resource-type: "secret"
