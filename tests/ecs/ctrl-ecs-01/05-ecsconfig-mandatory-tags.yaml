# AC-11: local ECSConfig with mandatory.tags that should merge with KropathConfig.mandatory.tags.
apiVersion: aws.kropath.run/v1alpha1
kind: ECSConfig
metadata:
  name: general-policy-ac11
  namespace: payments-prod
spec:
  mandatory:
    tags:
      service-type: containers
  defaults: {}
