# Assert AC-11: defaults.namingTemplate="{namespace}-{name}" from level-7 global EKSConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: EKSConfig
metadata:
  name: general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    defaults:
      namingTemplate: "{namespace}-{name}"
