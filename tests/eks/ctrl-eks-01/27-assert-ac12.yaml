# Assert AC-12: effCfg.mandatory.tags is the union of KropathConfig.mandatory.tags
# and EKSConfig.mandatory.tags.
apiVersion: aws.kropath.run/v1alpha1
kind: EKSConfig
metadata:
  name: general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    mandatory:
      tags:
        cost-centre: platform
        service: eks
