# Assert AC-6: mandatory.loggingTypes=["api","audit"] from level-1 KropathConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: EKSConfig
metadata:
  name: general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    mandatory:
      loggingTypes:
        - api
        - audit
