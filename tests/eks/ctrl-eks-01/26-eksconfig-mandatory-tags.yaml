# AC-12b: EKSConfig with mandatory.tags (level 4).
apiVersion: aws.kropath.run/v1alpha1
kind: EKSConfig
metadata:
  name: general-policy
  namespace: payments-prod
spec:
  mandatory:
    tags:
      service: eks
