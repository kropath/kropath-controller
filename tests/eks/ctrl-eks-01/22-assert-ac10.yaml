# Assert AC-10: defaults.supportType="STANDARD" from level-7 global EKSConfig.
apiVersion: aws.kropath.run/v1alpha1
kind: EKSConfig
metadata:
  name: general-policy
  namespace: payments-prod
status:
  effectiveConfig:
    defaults:
      supportType: "STANDARD"
