# AC-10: global EKSConfig with defaults.supportType="STANDARD" (level 7).
apiVersion: aws.kropath.run/v1alpha1
kind: EKSConfig
metadata:
  name: general-policy
  namespace: kro-system
spec:
  defaults:
    supportType: "STANDARD"
