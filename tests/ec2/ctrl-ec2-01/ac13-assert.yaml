apiVersion: aws.kropath.run/v1alpha1
kind: EC2Config
metadata:
  name: ac13-general-policy
  namespace: network-prod
status:
  effectiveConfig:
    mandatory:
      syncedLabels:
        environment: prod
    defaults:
      syncedLabels:
        team: networking
